# AGENTS

> 协作约定：回复用户时以「东家」开头；主体语言使用中文，技术术语、命令、API 名称、代码标识符保留原文。
>
> 全局规则：项目 CONVENTIONS 只能收紧、不得放宽本文件的任何规则。

## 工程原则

1. 先查证再动手：基于代码、配置、测试、文档的事实判断，不猜；信息不足先问。
2. 根因优先：在真正负责该行为的最小层级修复，不吞错、不绕过、不重复补偿。
3. 正确性优先于简洁：校验、错误处理、事务、数据完整性不可省。
4. KISS & YAGNI：选满足当前需求的最简单方案；不为假想需求加功能、抽象或配置。
5. 遵循现有约定：结构、命名、风格沿用项目现状，不凭偏好另起炉灶。
6. 聚焦修改：只改解决问题所必需的，不顺手重构无关代码。
7. 显式依赖与契约：依赖从 import/参数/构造可见；边界用签名与类型表达；注释只写代码说不出的原因与约束。
8. 命名表意：在所属领域内无歧义的最短名称；同一概念沿用既有 canonical term，不造同义词。
9. 行为变化必留可执行证据：最小可运行检查，断言可观察行为而非内部实现。

## 系统操作边界

- 写入范围：文件的创建与修改只允许发生在仓库工作目录内；禁止写系统路径（如 `/usr`、`/etc`、`/lib`、`/boot`、`/var` 或他人家目录），除非东家当次明确授权并指明目标路径。
- 提权与全局变更：禁止 `sudo`/`su`、修改系统服务、内核参数、动态链接器配置，以及面向系统环境的全局包管理（如 `apt`/`dnf`、`pip`/`npm -g` 装入系统 site 路径），除非东家当次明确授权；依赖一律安装进项目内隔离环境。
- 系统级实验：涉及容器镜像导入、驱动、网络、存储挂载等宿主机级操作，必须在隔离环境（容器、虚机）中进行，宿主机只作编排者。
- 不可逆操作：递归删除、终止系统进程、改动分区/挂载/防火墙等高爆炸半径操作，执行前必须逐项向东家确认。
- 授权语义：东家的授权当次、单动作有效，不构成后续同类操作的长期许可；范围之外一律停止并询问。

## 长时任务等待

- 禁止在执行命令前 sleep（如 `sleep 10 && make build`）；sleep 只允许出现在长时命令启动之后，作为轮询节奏的一部分。
- 长时任务（构建、测试、部署、下载、导入、滚动更新等）启动后，只能通过短轮询查询其执行进度：单次 sleep 不得超过 30 秒；每次醒来必须检查任务状态（退出码、进程存活、日志尾部、输出文件等），发现任务提前结束、报错或输出异常时立即停止等待并处置，不得继续等到预设时长。
- 工具自带带超时的阻塞等待（如 `timeout`、`helm ... --wait --timeout`、`kubectl rollout status --timeout`）且语义满足需求时，优先使用工具等待而非自写轮询。

## AgentMap 工作系统

AgentMap 是面向 agent 的活文档工作流：一套固定的项目级活文档构成项目的坐标系——PRD 定义意图，DESIGN 承接需求并指向实现，代码与测试提供执行证据。它取代碎片化的一次性需求 spec，使整体目标、当前进度、与目标的距离始终可查。

组成与分工：

- `PRD.md` —— 做什么、不做什么：项目愿景、目标（G）、非目标（NG）、按角色分组的需求（R）
- `DESIGN.md` —— 怎么做：架构视图（逻辑/部署/流程）、产品契约、需求追溯索引、子系统与模块、技术选型；唯一引用代码的文档层
- `DOMAIN.md` —— 统一词汇：术语、实体、关系、跨模块不变量
- `DECISIONS.md` —— 当初为什么这么定：决策记录（C），只追加
- `TODO.md` —— 还没决定的想法与备忘
- `CONVENTIONS.md` —— 怎么工作：本项目过程规范与 AgentMap 本地参数入口
- `tasks/` —— 每次变更的一次性方案与执行轨迹（T）
- `.githooks/` —— 验证门禁：提交时轻门禁（文档 lint、代码 lint、单元测试等快速检查），推送前重门禁（e2e、集成等完整验证）；测试按速度归类注册
- 代码 + 测试 —— 实现层：现在实际是什么；直接验证需求的测试锚定 AC-ID

各文件的编写规范见「文件规范」章。

每次新会话首次处理仓库任务前必须完整读取 `CONVENTIONS.md`；同一会话后续仅在该文件变化或相关主题需要时重读。

### 第一原则：逐层承接，逐层验证

- PRD 决定 DESIGN 要覆盖什么；DESIGN 决定代码如何实现；代码与测试裁决实现是否兑现 DESIGN。
- PRD 的需求完成设计验证，标准是 DESIGN 中恰有一行该 R-ID 的需求追溯索引，且主责子系统明确反向承接；PRD 不直接引用代码、测试、task 或 commit。
- map 与 code 不一致是显式信号，只有两种合法处置：收敛 code（走 task）；或经流程修 map（东家确认，必要时记 DECISIONS）。
- 两种非法处置：默默改 map 迁就 code；绕过 map 直接改 code。
- 需求层属主是东家：PRD 的任何演进必须经东家确认后方可继续。

### 入口甄别

收到消息后先判断意图类型，按下表确定路径；判断不了就问，不猜。未甄别直接动手即违反本协议。

- **新功能 / 需求变更**：PRD + DESIGN 演进；功能测试（EARS 派生）；先查重、查否决史。
- **缺陷修复**：map 不变；复现测试 + 回归；若发现 map 本身错 → 转需求变更路径。
- **性能优化**：PRD 目标已在则 map 不变，无目标先补承诺；基准测试；无基线不动手。
- **安全加固**：map 已有对应不变量则不变，否则先补；对抗性测试 / 威胁分析；补漏洞 = 缺陷修复，提水位 = 需求变更。
- **UI/UX 改进**：PRD + DESIGN 演进；人工验收为主；验收点先写成可判定形式。
- **技术迁移**：PRD 不变，DESIGN 演进到目标态；契约测试证明行为不变；分阶段，差距清单由 task 承载。
- **重构**：map 不变（DESIGN 对内部结构的描述被改变时同提交同步）；现有测试全绿。
- **想法 / 提问**：想法按格式进 TODO；提问只读作答，发现 map 与现实不一致时报告东家，不擅自改。

### 主工作流

1. **甄别定类**（见入口甄别）。
2. **查重**：PRD 检索相同/类似/相关需求 → 有则转为变更既有需求。
3. **查历史**：DECISIONS 检索是否否决过 → 被否决的须带新证据向东家申请翻案，不得直接立项。
4. **查现状**：DESIGN 与代码中是否已部分实现（代码可能跑在 map 前面）。
5. **map 演进**：PRD 演进 → 东家确认（闸口）；DESIGN 演进 → 东家确认（闸口）。map 不变的类型跳过本步。
6. **形成 task**：差距评估 + 收敛方案 + 测试计划。
7. **实现**：测试先行，锚定 R-ID。
8. **验证**（黄金标准，三条件缺一不可）：PRD→DESIGN 双向追溯完整；DESIGN 与实现对照无差异；每个 AC-ID 均有测试锚点且全部通过。
9. **关闭 task**：agent 判断计划内实现已完成且测试结果足以证明任务目标后，将状态改为 completed 并填写实现、测试与 commit 证据；task 还须记录独立代码审核 `review` 证据。终态后不可再修改。

### 三种形态

- **全链**：map 演进类（新功能、需求变更、UI/UX、提水位类性能/安全），走 1–9 全部。
- **短路**：map 不变类（缺陷修复、重构、达标类性能/安全），跳过第 5 步；可复现的缺陷修复与行为不变的修正可不开 task，但必须附回归测试。
- **分阶段**：技术迁移。PRD 不动，DESIGN 先写目标态；差距清单由 task 承载，是唯一允许长期 gap 合法存在的形态。

### 全局纪律

- PRD/DESIGN/DOMAIN 是活文档：原地改写，永远描述当前真相，不留历史；历史由 DECISIONS 与 tasks/ 承载。
- 行为不变的变更可短路；任何改变可观察行为、接口、数据结构的变更必须走全链，不得"大改小报"。
- 引用：需求、决策与 task 使用稳定 ID（R/G/NG/C/T-nnn），设计元素用 DOMAIN 规范名；禁止引用段落标题。
- 验收点使用 `<R-ID>/AC-nn` 作为稳定 AC-ID；直接验证需求的测试必须锚定对应 AC-ID，可机械筛选。
- DECISIONS 只追加。task 在 active 期间可编辑；状态只能从 active 转为 completed / abandoned / superseded，进入终态后不可再修改。
- completed 表示执行 agent 判断任务实现完成且已经测试，不设置 verified 状态；独立代码审核 `review` 是完成证据之一，不取代执行 agent 的关闭职责。
- 需求变更/删除原子级联：PRD、DESIGN、代码与当前测试在同一次变更内同步；task 是审计历史，不因 R-ID 退出当前 PRD 而清除引用，受影响的 active task 同次更新或转为 superseded。
- 规范优先做成可执行检查（hook/lint/脚本），文档只承载执行不了的部分。
- `.githooks/pre-commit.d/*.sh` 与 `.githooks/pre-push.d/*.sh` 使用同目录唯一的 `NN-name.sh`；编号按预期成本由低到高排列，dispatcher 在首个失败处停止。
- PRD/DESIGN 演进串行；代码实现可并行。
- 核心文档（PRD+DESIGN+DOMAIN+本节）总量接近 2 万 token 时触发结构评审；在出现真实阅读或并发冲突前不预设分片目录。
- 各文件的编写规范以「文件规范」章为唯一载体；文件内不再内嵌使用指南。

### 文件规范

各文件只含 frontmatter、标题与正文；使用规范集中于此章。项目特定的取值（需求组映射、编号起点等）记录在 CONVENTIONS.md 的「AgentMap 本地参数」中。

- **写作风格**：长句或长段优先用列表或嵌套列表结构化表达，每条保持简短（一句话或一个短语）；不得把大段内容平铺在一个超长句或超长段里。适用全部活文档。

#### PRD.md

PRD 是需求层文档：记录项目为什么存在（愿景）、要达到的可判定状态（目标）、显式排除的方向（非目标），以及每类角色可观测的系统承诺（需求）。它回答"做什么、不做什么"，不回答"怎么做"（那是 DESIGN）。

- **读**：从上往下（项目愿景 → 目标 → 非目标 → 需求清单），条目冲突时以上层为准；"关联目标/关联设计/出处"为空表示尚未建立，不表示需求无效；本文件不出现代码、测试、task 或 commit。
- **判归属**：替换测试（换种机制而承诺不变 → 属 DESIGN）；受众测试（该角色的集成、操作或决策需要感知 → 属本文件）。接口形状（端点/参数名/状态码）与机制一律不写入。
- **编号**：统一使用 `R-gg-nnn`。两位 `gg` 是需求组，按确认角色在 PRD 中首次出现的顺序从 `01` 连续分配，并以 `需求组 gg: <角色>` 登记在 CONVENTIONS；需求组登记项永久保留。三位 `nnn` 在组内从 `001` 递增。需求组和需求编号删除后永不复用，角色改名或需求转移也不改既有 ID。
- **条目字段**：`#### R-gg-nnn <能力名>` + 陈述（角色+情境+结果+保障，用 DOMAIN 词汇）+ 验收点（以 `AC-01` 起编并与 R-ID 组成 `<R-ID>/AC-nn`；删除后不复用；正文采用 EARS 五句式且每条必须能被测试判定真假）+ 关联目标（G-n，主归属一个）+ 关联设计（DESIGN 的子系统规范名）+ 出处（C-nnn，可选）。用户或运维可观察的性能、可靠性、安全、兼容性和恢复承诺也属于需求，不得只写在 DESIGN。
- **示例**（虚构，仅示范格式）：

```
#### R-01-099 搜索结果导出
集成方在检索后可要求将结果导出为文件，以便离线分析；导出内容不含系统内部信息。
- AC-01 当导出目标不存在时，系统应当明确报错而非返回空文件。
- 关联目标: G-1
- 关联设计: 文档服务
- 出处: C-099（可选）
```

- **设计验证**：每条 R 必须关联一个 DESIGN 子系统，DESIGN 必须有恰好一行该 R-ID 的需求追溯索引，索引主责子系统与 PRD 一致且该子系统反向出现该 R-ID；由 `agentmap_lint.py` 机械验证。
- **纪律**：删除需求时，同一次变更内级联删除关联的 DESIGN 内容、代码与当前测试；执行删除的 task 和既有 terminal task 可保留被撤销的 R-ID 作为审计记录。

#### DESIGN.md

DESIGN 是设计层文档：记录系统如何组织——逻辑分层、子系统与组件、物理部署、核心模型、生命周期、数据流、时序、产品契约、横切约束和技术选型。它回答"怎么做"，不回答"为什么做"（那是 PRD）和"当初为什么这么定"（那是 DECISIONS）。

- **读**：理解系统时先读架构、关键流程与子系统；从具体 R-gg-nnn 出发时先查需求追溯索引，再读其指向的设计落点与实现位置。条目标注的 R-gg-nnn 是需求来源，C-nnn 链接是选型理由；代码位置引用只存在于本文件；迁移期间本文描述目标态，现状以代码为准。
- **判归属**：只写"怎么做"；需求理由 → PRD，决策过程与被否方案 → DECISIONS，术语定义 → DOMAIN。对外接口的精确形状集中在"产品契约"，内部组织在"子系统与模块"。
- **视图清单**：存在需求时，`架构视图清单` 必须逐项评估系统上下文、一级静态分解、内部组件分解、运行时交互、数据与领域模型、状态与生命周期、数据流与信任边界、部署、分层与依赖、系统景观。系统上下文与一级静态分解必须适用；简单项目可用紧凑 Markdown 表格表达这两个基础视图，关系或交互较复杂时使用 Mermaid；其余适用视图提供 Mermaid 图，不适用则写具体理由。
- **选择原则**：静态结构优先采用 C4/arc42 式逐级下钻，不在一张图混合抽象层级；运行时只覆盖具有架构意义的正常、失败、超时或降级场景；可部署系统必须提供部署图，敏感数据或跨信任边界系统必须提供数据流与信任边界图。逻辑分层、状态机、模型图和系统景观只在系统确有相应结构时使用。
- **设计细化清单**：逐项评估边界与对外契约、核心数据与不变量、状态与生命周期、运行时/并发/失败语义、外部集成、配置与可变点、安全与信任边界、部署/迁移/恢复、兼容性与版本演进、可观测性与运维。前三项中的边界契约、核心不变量、运行时与失败语义必须适用；其他项适用则用 `path.md#标题` 指向设计落点，不适用则写具体理由。
- **实现就绪闸口**：DESIGN 获批前必须确认边界与契约、关键不变量、重大选择、目标实现归属、当前到目标的差距、可派生验证均已收敛。实现者若仍需猜测会影响外部行为、数据正确性、安全性或兼容性的事项，则不得标记通过；局部函数拆分、私有数据结构和惯用写法属于实现自由。
- **按需下钻**：小项目在 DESIGN 内联细节；内容过密时可链接专题 Markdown。专题文档逻辑上仍属于 DESIGN 层，DESIGN 必须索引它；不得另建与 DESIGN 竞争的真相层。
- **图文分工**：图必须使用 DOMAIN 规范名，明确边界、依赖、连接、数据方向、状态与调用顺序；文字补充图无法表达的不变量、失败语义与理由。不得用图复制需求正文，不得添加装饰节点，也不得把无关流程塞入一张不可读的图。
- **需求追溯索引**：固定为 `需求 | 主责子系统 | 设计落点 | 实现位置`；每个 R-gg-nnn 恰好一行，可指向多个落点或位置；只做导航，不重述需求、验收点或共享设计。
- **模块条目字段**：`### <规范名>` + 职责（明确列出承接的 R-gg-nnn）+ 关键内部结构 + 代码位置（实现证据）+ 实现端。职责与关键内部结构等内容较长时用嵌套列表逐条列举，每条简短。
- **示例**（虚构，仅示范格式）：

```
### 结果导出
- 职责: 将检索结果序列化为导出文件并流式返回（实现 R-01-099）
- 关键内部结构: 复用搜索管线，导出层只做格式化，无独立存储
- 代码位置: src/search/export.py
- 实现: 单端
```

- **纪律**：子系统与模块以 DOMAIN 规范名引用，改名必须全局同步；契约条目必须标注 R-gg-nnn，重要选型必须链接 DECISIONS；需求追溯索引只引用整体设计，不建立逐需求的重复设计正文；大规模迁移期间先写目标态，现状差距由对应 task 承载；迁移期 map 先于代码时，索引与模块条目的实现位置填目标路径（目标态描述），实现收敛后核实。

#### DOMAIN.md

DOMAIN 是领域词典：定义全仓库统一的术语、实体、关系与跨模块不变量。PRD 与 DESIGN 的用词以本文件为准；它不描述任何模块的实现方式。

- **读**：先术语表，再实体与关系；不变量是跨模块红线，冲突时不变量优先。
- **写**：只放术语表、实体与关系、生命周期、跨模块不变量；一句话无歧义、不含实现细节；同一概念全仓库只用这一个词；新词汇先登记再使用；术语改名必须在同一次变更中全局同步 PRD/DESIGN。
- **示例**（虚构）：`- **订单**: 一次已确认的购买请求，包含商品、数量与收货地址。`

#### DECISIONS.md

DECISIONS 是决策日志：记录重大决策（在候选方案间做出的选择）的上下文、选择、被否方案及原因。它回答"当初为什么这么定"，是唯一承载历史的文档（活文档只描述现在，历史由它和 tasks/ 承担）。

- **读**：编号即时间顺序；被废弃条目以最新决策为准，原文保留供追溯；每条价值一半在"被否方案及原因"。
- **编号**：新决策自 C-001 起使用 `C-nnn` 递增、不复用；升级前已被 Git 跟踪的字母后缀编号可保留，但不得新增。起始编号与迁移背景等项目特定信息记录在 DECISIONS.md 文件头部。
- **判门槛**：存在两个以上认真候选方案、且选择理由无法从结果反推时才写；只有一个合理选项的变更不写。
- **条目**：写于本文件末尾；四段式——上下文 / 决策 / 被否方案及原因 / 影响面（R-gg-nnn · 子系统规范名）。只有出现真实阅读或并发冲突时，才另立决策设计分片方案。
- **示例**（虚构，仅示范格式）：

```
### C-099 用单文件存储而非外部数据库承载任务队列
日期: 2026-08-01

#### 上下文
（促使必须做决定的处境与约束）
#### 决策
（选择了什么，一句话能说完）
#### 被否方案及原因
（每个认真候选：为什么输）
#### 影响面
R-01-012 / 任务存储
```

- **纪律**：只追加，写入后不可修改；废弃旧决策时新条目声明"废弃 C-nnn"；编号递增不复用。

#### TODO.md

TODO 是想法收集箱：存放想做但尚未决定的事。它不是需求（去 PRD），不是计划（去 tasks/），不记历史。

- **读**：纯文本清单，一条一行；每条开头的入口类型标签只表示初步甄别，不代表承诺或计划。
- **写**：每条使用且仅使用一个入口类型标签：`[需求候选]`（可能改变产品承诺）、`[缺陷线索]`（尚未确认的异常或漂移）、`[性能想法]`（尚无已承诺目标或基线）、`[安全想法]`（尚未确认的风险或加固方向）、`[维护想法]`（文档、工具链、配置或内部结构）；必要上下文在同一行括注。完成甄别后按对应工作流迁出 TODO；每次 PRD 演进时评审一遍，决定做就升级为需求（R-gg-nnn），决定不做就删除，不留坟场。
- **示例**：`- [需求候选] 为搜索增加结果导出（无客户提出，收益不明）`

#### CONVENTIONS.md

CONVENTIONS 是本项目的过程规范入口：记录在这个仓库工作必须遵守的开发约定，以及 AgentMap 的项目特定取值（本地参数）。它管"怎么工作"，不管"产品是什么"（那是 PRD/DESIGN）。

- **读**：新会话首次处理仓库任务前完整读取；之后先看高频规则区，再按主题索引跳转。文字规范与可执行检查（hooks/lint）冲突时以检查为准并修正文字。
- **写**：只放过程规范与本地参数，产品事实与产品设计 → PRD/DESIGN；高频短小内联，厚重主题一句话 + 链接；能写成脚本/hook/linter 的先落成可执行形式；`## 验证门禁` 必须声明权威验证入口、CI 适用性并逐条登记实际 hook；AgentMap canonical commit convention 始终生效，项目附加规则由 `commit-msg.d/` 承载；规范的新增与变更记一条 DECISIONS。
- **示例**（虚构）：`- Python 代码风格以 ruff 为准（pre-commit hook 强制检查）`

#### tasks/

- **写**：`tasks/T-nnn-YYYYMMDD-slug.md`，frontmatter `id: T-nnn` 必须与文件名一致；T-ID 按创建顺序取下一个空号、删除后不复用。内容 = 风险等级（`standard` / `high`）+ 背景与目标 / 差距评估 / 收敛方案 / 测试计划 / 验证矩阵 / 终态与证据。升级项目可在 CONVENTIONS 登记验证矩阵与代码审核的起始 T-ID；此前 task 不迁移。
- **代码审核**：实现 agent 在关闭 task 前必须调用 `code-review` skill，不能自行跳过或代替独立审核。reviewer 开始审核前，必须先充分了解被评审代码要解决的目的、关联的 PRD/DESIGN 约束、预期行为和验证方式，并将该目的理解记录在 `review` 证据中；未完成目的理解不得开始审核。审核发现问题时，执行 agent 修复后须由同一审核方复审，循环直至问题全部消失；completed 时在终态记录以下结构化 `review` 证据：
  - `审核方`: 独立 reviewer 标识
  - `目的理解`: 代码要解决的目的、关联约束与预期行为
  - `执行方式`: `code-review` skill、评审基线与范围
  - `问题与修复`: 每项 finding 及对应修复；无问题时明确写无
  - `复审结论`: 最终通过结论
  认证授权、资金、隐私、数据迁移、公开兼容契约或其他不可逆/高影响变更仍标为 `high`（风险等级标注保留，不改变审核要求）。所有 task 均须记录独立代码审核 `review` 证据，不区分风险等级。
- **验证矩阵**：固定表头为 `维度 | 适用性/理由 | 可执行证据`，至少包含成功、异常、边界配置、副作用四行；从 DESIGN 适用关注面补充安全、性能、并发、迁移、恢复、兼容性、可观测性等风险维度；仅在存在多个实现时增加跨实现。证据格式及充分性纪律保持不变。
- **测试锚定迁移**：新项目使用 `测试锚定模式: strict`；既有项目只有在 `CONVENTIONS.md` 使用 `测试锚定模式: migration T-nnn` 引用一个 active 技术迁移 task 时才可暂缓缺失锚点错误，补齐后先切回 strict 再关闭该 task。
- **并发语义**：尚未进入 canonical branch（项目的集成基线）的 T-ID 是 provisional；一次只集成一个 incoming branch，先进入者先占号。集成前必须 fetch。
- **未发布冲突**：若 branch commits 不被任何 remote-tracking ref 包含，rebase 到最新 canonical branch；在原 commits 中，将本 branch 相对 merge-base 新增的全部 task 按原 T-ID 顺序，从 canonical 最大 T-ID 的下一个编号起连续重排，并同步文件名、frontmatter、branch 引入的引用与被改写的 commit 证据。
- **已共享冲突**：若 commits 已推送或共享，默认不重写；canonical target 保号，incoming tasks 按同一规则在 reconciliation commit 中整体重排。只有全部使用者同意时才 force-push。
- **重排边界**：纯编号迁移是 terminal task 不可变规则的唯一例外，只允许改变 T-ID 及其引用，不得改变状态、正文语义或其他证据；完成后必须运行 lint。
- **生命周期**：frontmatter 使用 `mutation: lifecycle`。active 期间允许持续编辑；只能从 active 转为 completed（执行 agent 判断实现完成、已经测试且 DESIGN 与结果实现一致，并填写 `实现`、`测试`、`DESIGN 对照`、`commit` 四行证据，commit 为已存在的 Git hash；无需东家或独立 agent 确认）、abandoned（`原因`）或 superseded（`后继`）；不增加 verified 状态；进入终态后不可再修改、删除或移动。升级前已终态 task 不追补新证据。completed task 应在实现提交后的独立提交中关闭，避免引用尚不存在的 commit。
- **纪律**：引用 task 时使用 T-ID；与 DECISIONS 共同构成"需求变更 → 代码变更"的审计链。升级前已被 Git 跟踪的旧格式 task 保持原名和原内容，不补 ID。
