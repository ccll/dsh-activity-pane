---
doc-type: decisions
mutation: append-only
id-prefix: C
owner: agent 主笔，项目属主审批
---

# DECISIONS — 决策日志

### C-001 新建独立插件仓库，而非 fork dsh-answer-pet
日期: 2026-08-20

#### 上下文
需要把 dsh-answer-pet-companion 的能力改造成「活动会话总览窗格」，产品形态与桌面宠物完全不同，且有大量自定义设计理念。存在「fork 上游完整历史」与「新建独立仓库、借鉴部分代码并声明来源」两条路线。

#### 决策
新建独立仓库 dsh-activity-pane：不 fork 上游历史，只借鉴多会话卡片布局等少量 MIT 代码，并在 README 与 LICENSE 中声明来源与归属。

#### 被否方案及原因
- fork dsh-answer-pet 完整历史：会让仓库身份被锁在桌面宠物上；上游对主题引擎/卡片 CSS 的演进会在我们重写过的文件上反复冲突；窗格插件的真正集成对象是 DSH Web 外壳（AppFrame/槽座），与上游宠物仓库内部结构无关。

#### 影响面
R-02-001 / 活动状态模型、R-02-003 / 活动状态模型；仓库形态、归属与许可管理

### C-002 数据源采用 DSH 原生会话/工作区服务，而非继承第三方状态路由
日期: 2026-08-20

#### 上下文
companion 的进度数据来自第三方插件的 `/answer-pet/state` HTTP 路由与轮询；新插件要完全独立、不依赖第三方宠物插件。

#### 决策
只订阅 DSH 原生 `sessions` / `workspaces` 客户端服务（推送式快照）取得运行、完成、等待行动与层级数据，不发起任何状态轮询，也无自有宿主路由。

#### 被否方案及原因
- 沿用 `/answer-pet/state` 轮询：引入对第三方插件的硬依赖，无法独立装/卸，且轮询带来无谓流量与渲染抖动；与「独立数据源」目标相悖。

#### 影响面
R-02-001 / 活动状态模型；R-01-001 / 活动状态模型

### C-003 桌面挂载采用「左栏旁新列」插入 AppFrame，而非右侧 details 槽或悬浮面板
日期: 2026-08-20

#### 上下文
桌面窗格需要贴边、不遮挡主会话；外壳已提供右侧 details 槽（可拖宽、狭窄时自动收起）与三栏 AppFrame；东家明确要求放在左侧栏旁的新列。

#### 决策
窗格作为 `#root [data-slot="conversation"]` 槽座的前置兄弟列插入外壳 flex 行，让让步链挤压中间栏；窄屏（≤767px）切为固定抽屉 + 浮动开关。

#### 被否方案及原因
- 复用右侧 details 槽：外壳现成、代价低，但位置在右、不满足东家「左侧栏旁新列」的形态要求。
- 独立悬浮覆盖面板：与移动端浮层同源，会遮挡主会话，违背「不遮挡」目标。

#### 影响面
R-01-007 / 窗格渲染器、R-01-008 / 窗格渲染器、R-02-002 / 窗格渲染器

### C-004 桌面窗格以"中间列内真实 flex 行元素"实现，弃用浮层与 margin 推挤
日期: 2026-08-20

#### 上下文
C-003 计划在槽座前置插入列；实现中发现外壳 AppFrame 是 CSS Grid 三列，`[data-slot="conversation"]` 为 `display:contents` 无盒占位、其父级是中间列（flex column）而非 grid 行。先试"绝对定位浮层 + 会话座 margin 推挤"：槽座无盒导致 margin 不产生位移，主会话纹丝不动、窗格像盖子覆盖并拦截主会话区点击；聊天列还出现贴左不居中。东家明确要求：窗格必须真实占据空间、与其它元素一起参与布局，禁止浮层。

#### 决策
桌面下把中间列临时改为行方向，窗格作为其内真实的 flex 行子项（先于会话座）占据左侧固定宽（默认 280px）；经祖先链（跳过 `display:contents`）找到会话根并设 `flex:1 1 0%` 弹性填充余宽，会话内容（标题/tabs/滚动区/输入框）随窗格展开让位、随折叠（窄条）同步恢复；聊天列在增宽的会话带内自动居中。仅桌面生效：移动端抽屉以 `position:fixed` 脱离文档流，并恢复外壳默认列布局。卸载时复位对中间列/会话根的改写。

#### 被否方案及原因
- 绝对定位浮层 + 会话座 margin 推挤：槽座 `display:contents` 无盒，margin 无位移 → 内容不动、窗格覆盖、点击被拦截；与"真实占据空间"相悖。
- 收紧聊天列 max-width（748→620）：根因是会话根未弹性填充（缩至内容最小宽）而非列宽，方向错误无效。

#### 影响面
R-01-007 / 窗格渲染器、R-01-008 / 窗格渲染器、R-01-011 / 窗格渲染器

### C-005 富卡内容经客户端原生订阅快照复刻 answer-pet 卡片，而非移植宿主侧事件折叠 + HTTP 路由
日期: 2026-08-21

#### 上下文
东家要求活动卡片内容向 answer-pet 的卡片靠拢（阶段文案、工具白名单参数摘要、token 计数与速率、阶段进度、最近流程节点轨迹）。调研确认 answer-pet 的 `/answer-pet/state` 本身源自 DSH 原生 `session/event` 事件流（宿主侧折叠 progress/trace 后经 HTTP 轮询下发），不含任何私有数据；存在两条复刻路线。

#### 决策
不移植 answer-pet 的宿主侧会话事件折叠与自有 HTTP 路由，而是从纯客户端原生订阅快照复刻等价字段：
- 阶段/工具名/参数摘要：`sessions.binding(id).session` 的 `runningCalls`（name/argsRaw）、`partial`（stream/reasoning 块）、`turnTimings`（回合时长）；
- 已定案流程节点：`chat.legacy.nodes` 中的工具调用节点（`call.name`/`call.argsRaw`/`callTime`/`time`/`isError`）；
- token/速率：`sessions.list` 条目 `projectionValues.tokenUsage`（累计输出/输入）与 `sessionStats`（decodeTokens/decodeMs 吞吐），复用既有列表订阅，无新增轮询；
- 阶段进度：由阶段 + 输出 token 累计估计，渲染层按回合叠加单调下限，同回合不倒退。

#### 被否方案及原因
- 移植 answer-pet 宿主侧 `session/event` 折叠 + 自有 `/answer-pet/state` HTTP 路由：字段最精确（逐 chunk usage、速率 EMA、textSnippet 都在宿主侧可算），但要求引入 Node 宿主半端并维护自有状态路由与轮询，违背 NG-4 与 R-02-004「无自有路由、无状态轮询」纪律；且客户端订阅快照已带齐等价原语，精度损失仅外观级（token 累计口径、速率/进度近似、trace 由节点派生而非逐 chunk 重建）。

#### 影响面
R-01-009 / 活动状态模型、R-01-009 / 窗格渲染器

### C-006 最近卡片「最后做的事情」取列表投影 timelineUserMessages，投影缺失时优雅降级
日期: 2026-08-21

#### 上下文
东家要求历史会话卡片的信息更全面：补上工作区名称与「最后做的事情」。工作区名称缺口在于 `buildRecent` 已计算 `workspaceTitle` 但最近卡骨架从未渲染徽标（收口 R-01-003/AC-03 的实现缺口）。「最后做的事情」需要新的数据源：CLI 侧会话摘要不携带消息正文，运行期对非活动会话的 `binding().session` 是 cold（窗口未开、无 nodes），无法在不打开会话窗口的前提下读取最近一条用户消息；而宿主 `sessions.list` 条目本就推送 `projectionValues`（含第三方时间轴插件 `dsh-session-timeline` 注册的 `timelineUserMessages`：最近用户消息文本 + 回复预览）。

#### 决策
- 工作区名称：在最近卡骨架补 `.dap-workspace` 徽标元素，已有 `workspaceTitle` 直接上卡（实现缺口收口，不新增数据流）。
- 最后做的事情：新增核心纯函数 `lastActivityPreview(projectionValues)`，从列表条目 `projectionValues.timelineUserMessages` 取 **`seq` 最大条目**（不假设数组顺序）的用户消息文本（`text`），缺失时回退该条回复预览（`reply`），经 `cleanPreview` 截断后上卡；并入 `cardSignature` 触发去重重绘。
- 该投影经既有列表订阅推送（无新增轮询、无自有路由），仅有该投影的插件未装入时不产生该 key，卡片退化为仅显示时间；以下一次渲染的自然降级为边界，不报错。命名与语义保持「可留意」，但对缺失的容忍完全兼容 R-02-001「未装第三方插件仍可用」：窗格整体功能不依赖该 key 存在。

#### 被否方案及原因
- 原生 `sessionStats` 摘要（共 X 轮 · Y 步）：纯原生、无第三方依赖，但只是工作量统计，不是「最后做了什么」的消息级内容，与东家意图不符。
- 打开发行会话读取最近消息节点：能拿到消息正文，但要求对每个历史会话执行 `session.open()`/拉历史分页，违背「不主动轮询、轻量常驻」的既有纪律，且一次性拉取多会话历史开销不可控。
- 仅靠 `updatedAt` 时间行：已在卡片上，不构成「最后做的事情」。

#### 影响面
R-01-010 / 活动状态模型、R-01-010 / 窗格渲染器；R-01-003/AC-03 / 活动状态模型、R-01-003/AC-03 / 窗格渲染器

### C-007 撤回 T-004 最近卡增强，恢复基础历史卡行为
日期: 2026-08-21

#### 上下文
东家反馈 T-004 的最近历史卡增强方案不理想，要求撤回。T-004 的实现提交为 `886d1ad`，关闭提交为 `dfdd24a`；代码、当前 PRD/DESIGN/DOMAIN 与测试需要回到 T-004 之前的基线 `b2b416f`。C-006 保留为历史记录，不改写决策日志。

#### 决策
废弃 C-006 对当前产品行为的影响，恢复 T-004 之前的最近历史卡：仅保留既有标题与最近时间呈现，不读取 `timelineUserMessages` 投影，不新增最近卡工作区徽标或最后活动预览。以新的撤回提交与 T-005 记录审计链，不通过改写历史 commit 消除 C-006/T-004。

#### 被否方案及原因
- 仅隐藏预览但保留 `lastActivity` 数据链：仍保留东家认为不理想的数据依赖与契约复杂度，未达到撤回目的。
- 直接删除 C-006 或 T-004：违反 `DECISIONS.md` 只追加与 terminal task 不可修改的审计纪律。

#### 影响面
R-01-010 / 活动状态模型、R-01-010 / 窗格渲染器；R-01-003 / 活动状态模型、R-01-003 / 窗格渲染器

### C-008 工作项与历史预览使用 DSH native conversation 数据，不重新依赖 timelineUserMessages
日期: 2026-08-21

#### 上下文

新的 R-01-012/R-01-013 要求活动卡复用主会话窗口最近 4 个工作项（含当前实时项），并要求历史卡分别显示最近用户消息与 agent reply 的第一个非空物理行。当前会话快照已经提供 `chat.order`、`chat.nodes`、`partial`、`runningCalls`；冷会话可通过 native `sessions.history` 只读读取原始事件，模型上下文可通过 native `sessions.models` 读取。旧 C-006/T-004 的 `timelineUserMessages` 投影会折叠空白且只表达用户消息/回复对，不能保证工作项顺序与物理首行。

#### 决策

使用 DSH native conversation snapshot、native history API 与 native model catalog 作为数据源：当前会话优先读 `chat.order`/`chat.nodes` 并由 `session.subscribe` 推送刷新；冷会话仅做一次 history/model 读取并在缺失时降级为空字段。不得新增第三方路由、不得把 `agentPreset` 当模型名称，也不重新引入 `timelineUserMessages` 作为必需依赖。

#### 被否方案及原因

- 复用 `timelineUserMessages` 投影：只能提供压平后的用户/回复预览，不能证明主窗口工作项的完整顺序，也丢失物理换行语义。
- 打开每个会话窗口再克隆主窗口 DOM：会改变当前会话选择并产生不可控的布局/网络副作用。
- 自建宿主 projection/HTTP route：会扩大插件部署边界，违背原生只读服务与无自有状态路由约束。

#### 影响面

R-01-012 / 活动状态模型；R-01-013 / 活动状态模型、窗格渲染器；R-02-004 / 窗格渲染器

### C-009 以分阶段大改收敛实现层缺陷与性能，而非推倒重来
日期: 2026-08-22

#### 上下文
窗格在实机使用中出现速度慢、流式期主线程压力大与多处奇怪 bug（重试链把用户拽回旧会话、模型/历史永久空白、契约漂移死代码等）。东家要求重新评估实现方案，在「推倒重来」与「保留骨架分阶段收敛」之间做选择。

#### 决策
保留「推送订阅 → 纯函数归一 → 签名去重 → 卡片复用」的数据流骨架与布局/重挂载机制，按缺陷批、性能批、加载态需求（R-01-014）分阶段收敛；不推倒重来。

#### 被否方案及原因
- 推倒重来：现有难点（桌面真实布局参与 C-004、按下/抬起间保 click 的 DOM 复用、外壳重挂载自愈、推送纪律 C-002/C-005/C-008）均已磨合并被测试锚定，重写需重踩同样的坑；评估确认问题集中在实现层（全量重算、记账永久化、契约漂移、加载态缺口），骨架无缺陷，重写无新候选路线。

#### 影响面
全局 / 活动状态模型、窗格渲染器

### C-010 运行卡以工作项时间线承载运行轨迹，删除独立的流程节点轨迹 trace
日期: 2026-08-22

#### 上下文
DESIGN 与 PRD（R-01-009/AC-07、AC-09）描述运行卡显示 `buildTrace` 产出的「流程节点轨迹」（已定案工具调用 + 当前阶段），但渲染层实际以 `timeline?.length ? timeline : trace` 取舍，工作项时间线几乎恒非空，trace 被遮蔽——每次渲染仍为其付出 O(窗口节点) 计算，map 与 code 漂移。

#### 决策
删除 `buildTrace` 与条目的 `trace` 字段：运行轨迹由工作项时间线（R-01-012，含状态/耗时/白名单摘要/竖线圆点呈现）单一承载；R-01-009/AC-07、AC-09 改写为工作项时间线语义，`cardSignature` 移除 `trace` 分量。

#### 被否方案及原因
- 恢复 trace 与时间线双轨显示：信息冗余、卡片更高，且保留每帧 O(n) 的全窗口节点过滤；时间线已覆盖节点状态、耗时与白名单摘要，双轨收益不抵成本。

#### 影响面
R-01-009 / 活动状态模型、窗格渲染器

### C-011 废弃沿自 answer-pet 的工具参数白名单摘要口径，文字以主会话窗口为唯一真相源
日期: 2026-08-22

#### 上下文
R-01-009/AC-04、AC-07 的工具参数白名单摘要（不展示完整命令）沿用自 answer-pet 的 trace 口径（见 C-005 上下文），并非东家显式要求；由此非选中态 fallback 摘要与选中态原生行文字语义不同，部分动作（todo_write、ask_user_question、cordis/未知工具、错误态、工作区内绝对路径）两态文字不一致，违反 R-01-012/AC-03。东家明确：可以且应当展示原始命令。

#### 决策
删除白名单约束：fallback 摘要镜像主会话窗口 `deriveSummary` 语义（分工具类型参数键、bash 含 command、无命中取首个字符串参数值、剥离工作区前缀、取首行），并复刻 todo/ask keyed 行的标题与状态摘要、错误态输出首行；PRD 两条 AC 同步改写。

#### 被否方案及原因
- 保留白名单、仅对齐标题与路径相对化：摘要两侧语义仍不同（cordis/未知工具/错误态继续漂移），且东家已否定该安全姿态。
- 非选中态也读主窗口 DOM 克隆文字：违背「非当前会话禁读主窗口 DOM」约束（跨会话串线风险），维持静态镜像方案。

#### 影响面
R-01-009、R-01-012 / 活动状态模型、窗格渲染器

### C-012 工作项时间线移除行级耗时，对齐主会话窗口
日期: 2026-08-23

#### 上下文
工作项时间线镜像主会话窗口的工作项行，但插件额外在行尾显示耗时（仅工具行可由 `time - callTime` 得出，其余行硬编码 null），行与行之间有无耗时不一致；东家查证原生后主会话窗口任何行均无行级耗时（原生仅用于聚合统计），要求对齐。

#### 决策
工作项时间线不再显示行级耗时：`conversationTimeline` 产出的工作项不再携带 `durationMs`，渲染层删除行尾时间元素；R-01-009/AC-07 同步改写。卡片级已运行时长（`elapsedMs`）不受影响。

#### 被否方案及原因
- 保留工具行耗时：与主会话窗口行不一致，且同行时间线上部分行有耗时、部分行没有，视觉语义参差。
- 扩展为 assistant 行也算耗时（对齐原生轨迹页 `timing.stepStartTime` 推导）：偏离主会话窗口这一镜像对象，增加派生复杂度，东家未要求。

#### 影响面
R-01-009 / 活动状态模型、窗格渲染器

### C-013 完成提醒会话的处置语义：响应保持维持原视觉态，活动区→历史区迁移一律播放移动动画
日期: 2026-08-23

#### 上下文
东家反馈：点击完成提醒的活动卡后宿主立即清除 `completed`，卡片瞬间移入历史区，打断用户「查看后再决定如何处置」的上下文。由此确立响应保持行为（R-01-002/AC-05、R-01-010/AC-06/AC-07），其中两处存在认真候选方案需定夺：保持期间的视觉态，以及移动动画的适用范围。

#### 决策
- 响应保持期间维持「需要响应」标识与呈现样式不变，不降级为中性样式；用户一直停留在该会话则保持不解除。
- 移动动画适用于所有活动区→历史区的迁移，不限于响应保持解除这一种。

#### 被否方案及原因
- 保持期间降级为中性样式：削弱「该会话尚未处置完毕」的提示，且引入第三种视觉态增加辨识成本；东家选择简单无歧义的维持原状。
- 仅响应保持解除时播放移动动画：规则不统一——其它迁移（如选中态下运行结束落入历史区）同样瞬间消失，同样令人困惑。

#### 影响面
R-01-002 / 活动状态模型、R-01-010 / 活动状态模型、窗格渲染器

### C-014 运行卡进度改由本回合已耗时经有理曲线 y=t/(t+120s) 驱动，替代 token 累计饱和曲线
日期: 2026-08-24

#### 上下文
运行卡进度原由「阶段 + 输出 token 累计」估计（C-005），但 `tokenUsage` 投影只在 usage 事件（`assistant/chunk` 的 usage chunk / `assistant/message`）到达时批量落地：流式输出进行中 token 计数冻结，step 结束时数百 token 一次性入账，进度呈 10% 级跳变（5%→10%→20%→30%）。东家裁定底层驱动数据改为当前轮次耗费的时间；回合总时长同样不可知，需一条过原点、先快后慢、x→∞ 时渐近 100% 永不到达的时间曲线。

#### 决策
进度按 y = t/(t+120)（t 为本回合已耗秒数，半衰期 120s）映射，整回合单一曲线、不区分 think/stream/tool 阶段；替代 C-005 中「阶段进度由阶段 + 输出 token 累计估计」一项，C-005 的数据源路线（客户端订阅快照、无自有路由）不变。单调性由函数本身保证，渲染层单调下限（progressFloor）、tool 冻结语义与思考基线（PROGRESS_THINK_BASE）随 token 驱动一并移除；回合切换由 turnTimings 新回合起点自然归零重计。token 计数与速率显示（R-01-009/AC-05）不受影响。

#### 被否方案及原因
- 指数时间曲线 1−e^(−t/τ)：尾部指数趋近，τ=60s 时 5 分钟即钉死 99% 呈假死，长回合看似卡住。
- 反正切 (2/π)·arctan(t/τ)：形态与有理曲线近似，但参数无半衰期式直觉语义，且多一次三角函数计算。
- 保留阶段权重的分段时间填充（think 爬升/stream 饱和/tool 冻结）：tool 冻结期间条停住，平滑性打折，且阶段权重本身是任意常数。
- token/时间混合（stream 仍按 token）：stream 阶段的跳变根因依旧存在。

#### 影响面
R-01-009 / 活动状态模型、窗格渲染器

### C-015 README 只面向终端用户，开发工作流规范移入 CONVENTIONS
日期: 2026-08-24

#### 上下文
README 此前混写终端用户说明与开发说明（本地开发安装、构建/检查/热更命令、热更工作流与注意事项）；东家要求 README 只保留针对终端用户的说明，不再包含开发相关信息。

#### 决策
将 README「安装与开发」整节迁移至 CONVENTIONS 新增「构建与开发工作流」章节（本地开发安装、build/check/dev:watch 命令、热更流程与注意事项），README 删除该节，仅保留产品简介、感谢与协作声明、相比 answer-pet 的变化与状态。

#### 被否方案及原因
- 安装命令保留在 README 并改写为面向用户：当前唯一安装方式是从源码仓库以 `link:` 挂载（开发安装），尚无面向终端用户的发布安装方式，写入 README 可能误导为正式分发渠道。

#### 影响面
无 R-ID；CONVENTIONS / README

### C-016 折叠时间线以 vendor 数据层移植复用 dsh-auto-collapse 分组规则，而非运行时调用或读取其 DOM
日期: 2026-08-24

#### 上下文
东家要求活动卡时间线在 dsh-auto-collapse 存在并生效时同步其折叠呈现（组标题 + 推理文本内容，无需展开能力）。查证：其 client bundle 仅导出 apply/inject/name，findBlocks/buildSegments/FoldController 全部闭包私有；DSH client-modules 将跨插件 value import 明确标注为 forbidden；其折叠结果只存在于主窗口 DOM 的私有类名 chip 中，且仅覆盖当前打开的会话。

#### 决策
vendor 其 MIT 源码中的分组纯逻辑（findBlocks 的块识别与 updateChip 的标题/摘要优先级），移植到 ChatSnapshot 数据层成为 core 纯函数 `foldedConversationTimeline`/`foldWorkGroups`；检测只做只读样式标记探测（`#dshcf-style[data-dshcf-state]`），未检测到即回退 R-01-012 逐项镜像；在 DESIGN 横切约束与 README 保留来源声明。

#### 被否方案及原因
- 运行时 require 已安装插件：导出面上没有折叠逻辑，且平台约定禁止跨插件 value import。
- 解析其 DOM chip 上卡：仅覆盖当前选中会话（其余卡片无 DOM 可读），且耦合第三方私有类名，对方升级即静默失效。
- 不看源码自研同语义分组：其乱序排列回归等边角规则从行为反推易漏，vendor 移植成本更低、规则更确定。

#### 影响面
R-01-017 / 活动状态模型、窗格渲染器

### C-017 时间线只保留折叠分组呈现，移除逐项镜像与 dsh-auto-collapse 检测切换
日期: 2026-08-25

#### 上下文
R-01-017 落地后东家实际使用中始终偏好折叠分组呈现；逐项镜像只在未装或未生效 dsh-auto-collapse 时出现。双路径使 core 维持两套时间线与槽位派生家族，client 维持生效探测、memo 键切换维度，以及一套仅服务逐项镜像的宿主 DOM 匹配/图标克隆呈现机器（约 200 行）。而折叠分组本身是 vendor 数据层移植（C-016），不依赖该插件存在，检测切换并无功能必要性。

#### 决策
工作项时间线一律采用折叠分组呈现：删除逐项时间线家族（`conversationTimeline`/`conversationTimelineWithSlot`/`historyTimelineWithSlot`/`SLOT_SCAN_BUDGET`）、dsh-auto-collapse 生效探测与条件切换，连同失去消费者的原生行匹配/图标克隆机器；core 以 `conversationWorkItems` 扁平内核作为折叠输入与分组成员级观察接缝。R-01-009/R-01-012 相关 AC 同次改写为分组行语义。

#### 被否方案及原因
- 保留检测仅把默认切为折叠：双路径与全部维护成本原样保留，只换默认值，收益不成比例。
- 仅删数据路径、暂留 DOM 匹配死代码：消费者已不存在，留置即成无守卫死代码，且本次收口后无回归风险面。

#### 影响面
R-01-009、R-01-012、R-01-017 / 活动状态模型、窗格渲染器

### C-018 用户指令行标识以整行浅绿平底取代虚线 outline
日期: 2026-08-25

#### 上下文
R-01-018 落地时以 1px 虚线 outline 标识指令槽位与时间线用户行，东家实际使用反馈虚框过丑且过于抢眼，要求换成不抢眼的提醒视觉。蓝色已被 running 状态圆点占用（东家明确不可再用于用户消息），绿/红/橙亦各有状态语义，标识手段须在状态色体系之外或与既有语义相容。

#### 决策
槽位与时间线用户行以图标+文字整行浅绿平底标识（#58c98f 透明度 10%、4px 圆角）；全部时间线图标统一 14px 盒 + 1px padding 保证字形 12px、圆心对齐；助手正文行（robot）不设底色。

#### 被否方案及原因
- 图标或轨道圆点变色：占用/混淆状态色语义（蓝=进行中、绿=完成），东家明确否决蓝色用于用户消息。
- 图标圆形浅底：与用户行并存时感知尺寸不一致（亮度辐照错觉 + 色环厚度差异），且圆形与状态圆点同形易混。
- 圆环/圆角方环：14px 盒内字形顶边显挤；1px 描边在小尺度下辨识度不稳定。
- 左起 80%→0 渐变底：左缘过实、强弱不均，东家试看后否决。
- fit-content 紧凑色块 + 直角：东家试看后认为不如整行圆角平底。
- 助手行同系弱蓝/紫底：与用户底区分度不足或引入新色相噪音，最终只保留用户行标识。

#### 影响面
R-01-018 / 窗格渲染器、活动状态模型

### C-019 删除指令槽位功能；时间线用户行标识由浅绿平底改为行下中性灰虚线下划线
日期: 2026-08-26

#### 上下文
东家实际使用后判定时间线顶部的常驻指令槽位（R-01-018）没有存在价值，要求彻底删除——含围绕它的槽位派生家族、最近指令记账与运行卡轻量 history 拉取；同时否定时间线内用户消息行的整行浅绿平底标识（C-018），要求改为行下虚线下划线。注意 C-018 曾否决 1px 虚线 outline 整行框（过丑且过于抢眼），本次东家指定的是行下单边虚线下划线，属不同呈现手段，不构成对 C-018 被否方案的翻案。
#### 决策
- 删除 R-01-018 指令槽位需求及其全部实现：`.dap-slot` 渲染、`slotOf`/`windowHasUser`/`fallbackSlot`/`rememberLastUser`/`lastUserFromEvents` 派生家族、`foldedTimelineWithSlot`/`foldWorkGroupsWithSlot`（折叠时间线收敛为 `foldedConversationTimeline` 唯一入口）、运行卡轻量 history 指令拉取与 `lastUser` 记账；时间线 memo 键移除 lastUser 分量；DOMAIN「指令槽位」术语同步退役。
- 时间线用户消息行标识改为行下 1px 中性灰虚线下划线（深浅主题各自适配，不占用蓝/绿/红/橙状态色；以 bottom 背景渐变绘制而非 border-bottom，不改变 14px 行高几何），废弃 C-018 的浅绿平底呈现；C-018 的图标几何部分（真实 14px 盒、无占位 padding）继续有效。
#### 被否方案及原因
- 保留槽位仅弱化呈现或改为按需展开：东家明确要求彻底删除该功能，任何保留形态都违背意图；删除同时消除运行卡每会话一次轻量 history RPC 与槽位记账的复杂度。
- 用户行保留浅绿平底：东家明确否定该呈现，指定虚线下划线。
- 下划线使用状态色（蓝/绿/红/橙）：均已被运行/完成/错误/中断语义占用，会产生混淆；中性灰在状态色体系之外且深浅主题均可适配。
#### 影响面
R-01-018（删除）/ 活动状态模型、窗格渲染器；R-01-012 / 窗格渲染器

### C-020 历史区时间口径：插件侧以 turn/end 精化最后活动时间，取代宿主 updatedAt 口径
日期: 2026-08-26

#### 上下文
东家发现最近历史卡的「最近 xx:xx」显示的是宿主 `updatedAt`（会话创建时刻与最后一条真人用户消息时刻的较新者），而非会话真正最后更新的时刻；agent 回复结束、工具运行均不推进该值。东家判定「最后 agent 回复结束时刻」才是真实最后更新时间，要求历史区判定、排序与显示改用该口径。

#### 决策
- DOMAIN 重定义「最后活动时间」：最后一轮回复结束时刻与宿主列表时间的较新者；无已完成回合时即宿主列表时间；冷会话数据在途时先以宿主列表时间参与判定/排序/显示，到达后取较新者精化。
- 冷会话复用既有 history 拉取的同批事件提取最后 `turn/end.time`（不新增请求、不扩大深翻预算）；热会话取 `turnTimings` 最大 `endTime`；`buildRecent` 新增 `turnEnds` 入参归一 max；条目字段 `updatedAt` 改名 `activityAt`。
- 接受跨窗长回合缺口：宿主时间已超窗的会话不读取历史，其窗内回合结束时刻不可知、不入历史区。

#### 被否方案及原因
- 沿用宿主 `updatedAt` 口径：东家判定其失真——只反映发消息/创建时刻，回复跑多久都不推进。
- 严格只取回合结束时刻（不取较新者）：用户发消息后回合未结束（排队/立即中断）时最后用户消息本身即更新，回退到更早回合结束时刻反而更旧。
- 对超窗会话全量拉 history 消除跨窗缺口：需对所有超 24h 会话发起历史读取，违背「轻量常驻、有界读取」纪律，成本不抵边缘收益。
- 推动宿主修改 `updatedAt` 语义：宿主为上游 DSH，语义变更越出本仓库边界，且列表排序等其他消费方依赖现语义。

#### 影响面
R-01-010、R-01-013 / 活动状态模型、窗格渲染器

### C-021 机器人图标改为小电视几何并回归 12px 盒（废弃 Lucide bot 原版几何与 13px 字形）
日期: 2026-08-26

#### 上下文
东家在实际渲染中发现两个机器人图标问题：与同行文字居中对齐后视觉偏下（原单折线天线占格顶、头部与底座墨量偏下，墨质心低于几何中心约 1px）；且比相邻 canonical 图标模糊。查证定位模糊根因：13px 字形盒在 14px 图标盒内产生 0.5px 半像素居中偏移，所有描边边缘落在物理像素之间被抗锯齿抹灰；且机器人是全图标表唯一 stroke 描边图标（渲染 1.18px、无实心核），canonical 图标均为 fill 实心剪影。原版几何为东家此前选定的 F 方案（Lucide bot 原样几何 + 13px 字形盒），选定当时未经实际渲染对齐与清晰度检验。
#### 决策
- 机器人图标以 Lucide bot 几何为底改造为小电视式：去双耳，头顶单折线天线改为两条 45° 外撇短斜线（`M10 8L7 5` / `M14 8L17 5`），机身圆角矩形与双眼竖线不变。
- 字形回归与其他时间线图标一致的 12px 盒（消除半像素偏移）；stroke-width 2 提为 2.2，12/22 缩放后渲染笔触 1.2px，与 canonical 填充轮廓（约 1px 实心环）视觉重量相当。
- viewBox 保持 `1 3 22 18` 保框：机身显示尺度与改造前一致，不因裁小框而变粗变大。
#### 被否方案及原因
- 保留原几何 + translateY(-1px) 墨迹补偿：现场实验后东家判定不治本——对齐观感依赖补偿值，且半像素模糊原样保留；该实验已回退，未入库。
- 裁 viewBox 到新笔墨边界盒（约 `3 4 18 17`）：显示尺度突增约 22%、描边渲染变粗至约 1.4px，与 canonical 图标视觉重量失衡。
- 14px 满盒字形：偏移为整数且更清晰，但比相邻图标明显大一圈，破坏同行尺度一致性（R-01-012/AC-11）。
- 重绘为 fill 实心几何：模糊主因已由 12px 盒消除，实心重绘的成本与风格漂移风险不抵收益。
#### 影响面
R-01-012 / 窗格渲染器

### C-022 时间线新增指令锚行；用户行下划线改实线且仅覆盖内容宽度
日期: 2026-08-26

#### 上下文
东家使用反馈两点：(1) 用户消息行的虚线下划线贯穿整行，消息较短时右侧大片空白也带线，虚线观感偏碎；(2) 时间线只显示最近 4 个显示行，本轮指令被新动作挤出窗口后，活动卡失去「正在做什么」的语境。C-019 已删除时间线上方的常驻指令槽位（常驻显示最新指令），不能简单复活；下划线选型史见 C-018→C-019。

#### 决策
- 用户消息行下划线改为实线，宽度收缩为图标+文字的内容宽度（width: fit-content、max-width: 100%，仍以 bottom 1px 背景渐变绘制不占盒高）；部分修订 C-019 的整行虚线呈现，其余结论（中性灰配色、背景渐变绘制、图标 14px 盒几何）继续有效。
- 新增「指令锚行」：当最近显示行窗口之前存在用户输入行时，取其中最近一条由 `foldedConversationTimeline` 标记 `anchor` 并前置到时间线首行固定显示；该消息仍在窗口内时不显示。渲染层按普通工作项行渲染，无独立 DOM 分支。

#### 被否方案及原因
- 复活 C-019 已删除的常驻槽位（时间线上方额外一行常驻最新指令）：与窗口内用户行重复、恒占垂直空间，C-019 已否决。
- position: sticky 吸顶于窗格滚动：卡片自身高度小，吸顶几乎不可观察，且引入与滚动容器的耦合。
- 锚行出现后下方缩为 3 行保持总高恒定：信息无谓减少，东家确认下方照旧最多 4 行。
- 无论是否在窗口内始终显示锚行：与窗口内用户行短暂重复，东家选择挤出后才出现。

#### 影响面
R-01-012 / 活动状态模型、窗格渲染器

### C-023 指令锚行计入时间线总预算：总行数恒不超过 4，新指令攀顶直接顶替旧锚（修订 C-022 的窗口名额决策）
日期: 2026-08-26

#### 上下文
C-022 落地后东家实际使用发现两点：(1) 锚行 + 下方 4 行合计 5 行，超出期望密度；(2) 新指令从末行逐行上移、到达第 2 行时与第 1 行旧指令锚行相邻并列，两条用户消息易混淆。C-022 曾否决「锚行出现后下方缩为 3 行保持总高恒定」（当时判为信息无谓减少），东家观察实际效果后推翻该结论。

#### 决策
- 指令锚行计入时间线总预算：总行数（含锚行）恒不超过 4——锚行出现时下方窗口收缩为最近 3 个显示行，无锚行时仍为最近 4 个。
- 更近的用户输入行到达收缩窗口首行时直接顶替旧锚行（旧锚消失、各行上移），总行数暂减为 3，随后续新工作项回填恢复；期间少于 4 行属正常（会话开始阶段本就有少于 4 行的过程）。

#### 被否方案及原因
- 维持 C-022 的「下方照旧最多 4 行」：东家实测后明确否定（总 5 行过密）。
- 新指令到达窗口首行时保留旧锚只隐藏其一：信息不完整且引入隐式规则，顶替语义更直白。
- 把总预算直接改为恒 3 行（无论有无锚）：无锚时（会话开始阶段）白白少一行历史，东家只要求「不超过 4」。

#### 影响面
R-01-012、R-01-017 / 活动状态模型

### C-024 模型上下文改经 modelDirectories 目录 store 订阅获取实时选择（一次性 RPC 仅作初值）
日期: 2026-08-26

#### 上下文
东家实测发现：会话过程中切换模型后，活动卡/最近卡的模型上下文永远停留在首次读取时的取值。查证确认宿主 `session.selectModel` 只改内存选择并保存默认值，不产生任何会话事件、投影或推送帧；卡片模型来自对每个可见会话仅一次的 `session.models` RPC（T-016 设计的「可见期内不热重试」），会话列表行不含模型字段，故无任何通道感知切换。

#### 决策
模型上下文采用「订阅推送 + 一次性读取初值」双轨：为每个可见主会话订阅原生 `modelDirectories` 服务的 per-session 目录 store（与主会话窗口 `/model` 弹窗、输入框模型座位共享同一 store，`select()` 成功即推送当前选择），推送到达即重归一并就地更新卡片；初值与失败语义完全沿用既有 `session.models` 一次性读取路径；服务缺失、会话无 scope 或订阅失败时退化为纯一次性读取。目录 store 只订阅、不调用 `load()`——`load()` 会推进目录的 generation 计数，与用户进行中的 `select()` 竞争会导致其 store 更新被「最新操作胜出」规则丢弃。

#### 被否方案及原因
- 回合边界重读 `session.models`：切换后要到下一回合开始才更新，与主会话窗口「选择即刻生效」的呈现不一致。
- 从 `request/context` 会话事件派生：数据只有 `{provider, model, contextWindow}`，无 reasoningEffort 与 catalog 显示名，且同样滞后到下一回合；仅反映「最后请求所用」而非「当前选择」。
- 周期性重读 RPC（轮询）：违反定时器纪律（R-02-001「不使用数据状态轮询」）。
- 订阅 store 并主动 `load()` 代替自有 RPC：如上所述会与 `select()` 竞争 generation，且重复原生条目的加载职责。

#### 影响面
R-01-012 / 活动状态模型、窗格渲染器


### C-025 回合进度半衰期按会话实测输出速率校准（k = clamp(120×90÷r, 60, 600)s），锚点期间冻结
日期: 2026-08-26

#### 上下文
C-014 的 y = t/(t+120) 以 80–100 tok/s 模型为隐含基准（半衰期 120s 对应典型回合约 4 分钟）。任务产出 token 量与模型速度无关，回合墙钟时长与速率成反比；现常用模型输出速率降至 20–60 tok/s，固定 120s 半衰期使进度 10 分钟内冲到 83%、30 分钟到 94% 并长期停留高段，与实际节奏严重偏离。

#### 决策
半衰期 k 按会话实测输出速率校准：k = clamp(120×90÷r, 60, 600) 秒（r 为 `sessionStats` 全会话累计输出速率 tok/s，无可用速率取默认 120）；曲线形状与 C-014 全部性质（过原点、先快后慢、渐近 100 不到达、不区分阶段）不变。k 随进度锚点生命周期捕获冻结——idle 转活动时捕获当前实测速率对应的 k，锚点期间（含委托周期内）保持不变以保证进度单调，锚点归零重计时按最新速率重新校准。

#### 被否方案及原因
- 仅全局调大半衰期常数（如 120→240）：对快模型进度偏慢，换模型后再次失配，无自适应性。
- 恢复 token 驱动或 token/时间混合：C-014 已否决——tokenUsage 批量落地导致 10% 级跳变的根因仍在。
- 每帧以最新速率即时计算 k：速率估计随 step 落地批量变化，锚点中途 k 增大将造成进度倒退，破坏单调性承诺。

#### 影响面
R-01-009 / 活动状态模型、窗格渲染器

### C-026 工作区徽标色相按工作区身份哈希派生（无状态），而非调色板取模或黄金角有序分配
日期: 2026-08-26

#### 上下文
R-01-003/AC-08、AC-09 要求工作区徽标颜色同一工作区恒同色、不同工作区可区分、跨刷新稳定。存在三条认真候选路线。
#### 决策
以工作区身份（路径优先、名称兜底）为唯一输入的纯函数派生色相：djb2 哈希 → 十二槽 30° 量化 + 哈希低位 ±10° 抖动，输出 [0,360) 整数；无状态、无持久化。
#### 被否方案及原因
- 固定调色板 + 哈希取模：色板内可保证区分，但工作区数超过色板或撞哈希时必然重复，且引入一套需逐主题校验的固定色值。
- 黄金角（137.5°）递推分配：区分度数学上最优，但依赖工作区出现顺序并需持久化映射，增删工作区后颜色漂移，不满足跨刷新稳定。
#### 影响面
R-01-003 / 活动状态模型、窗格渲染器
