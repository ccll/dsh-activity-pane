---
doc-type: prd
mutation: living
id-prefix: R
owner: 项目属主
---

# PRD — 活动会话总览窗格

## 项目愿景

在 DeepSeek Harness Web 中，把分散在侧栏各工作区里的"正在运行"与"完成一轮后等待你行动"的会话收拢到一个可贴边、可滚动的窗格里，让用户一屏掌握所有需要关注的会话，并可直接跳转，取代向上溢出的浮动会话面板与遮挡主会话的移动端浮层。

## 目标

- **G-1** 用户无需逐会话翻看即可掌握所有运行中与等待行动的会话，并可直达目标会话。
- **G-2** 插件仅依赖 DSH 原生会话与工作区服务，可脱离任何第三方宠物插件独立运行，卸载可逆。
- **G-3** 窗格的桌面与移动形态保证可用：独立滚动、不遮挡主会话、外壳重挂载后自恢复。

## 非目标

- **NG-1** 渲染会话完整正文与完整历史；窗格只展示有限的工作项与消息首行预览，并提供跳转。
- **NG-2** 在本初版内提供会话语义操作（重命名、取消、队列编辑、直接响应等待项）。
- **NG-3** 桌面宠物、主题动画等装饰性呈现。
- **NG-4** 复用或代理第三方插件的状态路由与轮询协议。

## 需求清单

#### R-01-001 活动会话总览
用户浏览 Web GUI 时可在窗格中一次看到所有正在运行、以及完成一轮后等待其行动的会话，并获知工作区归属与当前会话位置。
- AC-01 当存在正在运行或等待用户行动的会话时，系统应当逐一显示对应的活动卡片。
- AC-02 当没有任何活动会话时，系统应当显示明确的空态，而不是报错或空白。
- AC-03 当会话状态发生变化（开始运行、结束、出现等待行动、等待解除）时，系统应当自动更新窗格内容，无需用户手动刷新。
- AC-04 当窗格列头显示活动会话数量时，系统应当以「等待响应数/活动主会话总数」的分数形式将数量标识紧跟标题文字呈现。
- AC-06 当不存在活动主会话时，系统应当将数量标识显示为「0/0」。
- AC-05 当统计数量标识时，系统应当只统计主会话：分母为运行中与等待响应的主会话总数，分子为其中处于等待响应状态者；子代理不计入。
- 关联目标: G-1
- 关联设计: 活动状态模型

#### R-01-002 等待行动可见性
完成一轮或正在等待用户输入的会话应当被醒目标识，并说明等待类型，方便用户决定先行处理哪个会话。
- AC-01 当会话等待用户确认（待确认）时，系统应当显示"待确认"标识。
- AC-02 当会话等待用户审查或回复时，系统应当分别显示"待审查"或"待回复"标识。
- AC-03 当主会话完成一轮而尚未被打开时，系统应当以"需要响应"标识并采用区别于运行中的呈现样式；当该会话存在活动后代时，系统不显示"需要响应"标识、卡片不切换为等待呈现，后代全部结束后若完成提醒仍然成立则恢复显示。
- AC-04 当会话显示等待行动标识时，系统应当以与窗格整体主题协调的柔和配色呈现该标识，且与非等待状态保持可区分。
- AC-05 当用户打开处于完成提醒的会话且该会话仍为当前会话时，系统应当维持其"需要响应"标识与呈现样式不变。
- AC-06 当存在等待响应的主会话时，系统应当将数量标识呈现为与等待卡片一致的背景色与透明度、不带描边与外环，并施加不改变其底色与透明度的脉冲提示；不存在等待响应的主会话时不脉冲。
- AC-07 当等待响应主会话占活动主会话总数的比例上升时，系统应当单调加快数量标识的脉冲频率，且频率不超过上限——全部等待时达到该上限。
- AC-08 当主会话显示「需要响应」标识时，系统应当以与卡片标题状态点同频同相的脉冲闪烁呈现该标识；其他等待类型标识不闪烁。
- 关联目标: G-1
- 关联设计: 活动状态模型

#### R-01-003 层级与归属
用户应当能从窗格卡片分辨会话的层级关系（母会话与子代理）与所属工作区。
- AC-01 当会话存在直属子代理时，系统应当以嵌套缩进把子代理卡片显示在其母会话卡片之下。
- AC-04 当会话存在直属子代理时，系统应当在母会话卡片与每个子代理卡片之间显示与缩进层级对齐的连接线；多级子代理的连接线不得改变卡片可点击区域。
- AC-05 当母会话自身不处于活动状态但其直属或更深层子代理处于活动状态时，系统应当将该母会话保持运行中呈现显示在活动区，卡片外观不因自身回合结束而改变。
- AC-02 当子代理会话结束时，系统应当不再显示该子代理卡片。
- AC-03 当主会话可归属到某个工作区时，系统应当在其卡片上显示该工作区名称。
- AC-06 当窗格卡片显示工作区名称时，系统应当在名称文字之前显示与左边栏工作区条目相同的文件夹图标。
- AC-07 当窗格卡片显示工作区名称时，系统应当以不小于 10.5px 的字号呈现该名称。
- AC-08 当卡片显示工作区徽标时，系统应当以按工作区身份派生的稳定色相呈现徽标的图标、文字、底色与描边：同一工作区的全部徽标色相一致；工作区身份以其路径为准、无路径时以名称为准；色相派生为纯函数，不依赖工作区出现顺序、会话状态或持久化存储，页面刷新后保持不变。
- AC-09 当派生工作区徽标色相时，系统应当把色相投放到避开红色警戒区的十槽色相弧（30°/槽，槽位覆盖 40°–310°）并附加哈希抖动（±10°），使不同工作区的徽标颜色尽量分散可区分，且任何徽标色相不落入红色区。
- AC-10 当工作区徽标以派生色相呈现时，系统应当沿用 color-mix 透明度层次使徽标与窗格明暗主题保持协调，且不改变胶囊的几何尺寸与字号下限。
- 关联目标: G-1
- 关联设计: 活动状态模型
- 出处: C-026、C-027

#### R-01-004 数量规模与滚动
当活动会话数量较多时，窗格应当保持可读、可滚动，不把卡片顶出屏幕，也不遮挡主会话内容。
- AC-01 当活动卡片总高度超过窗格可视区域时，系统应当允许用户在窗格内滚动查看全部卡片。
- AC-02 当用户在窗格内滚动时，系统应当保持主会话内容的滚动位置不变。
- AC-03 当用户在窗格内滚动时，系统应当显示窗格滚动条；当滚动停止后，系统应当隐藏窗格滚动条。
- 关联目标: G-1
- 关联设计: 窗格渲染器

#### R-01-005 跳转到会话
用户激活某张活动卡片后应当切换到对应的会话。
- AC-01 当用户点击或键盘激活一张活动卡片时，系统应当将当前会话切换为该卡片对应的会话。
- AC-02 当所点卡片对应的会话尚未进入客户端会话列表时，系统应当等待列表就绪后完成跳转；当等待超过固定时限仍未就绪时，系统应当结束本次交互，且不影响其它卡片。
- 关联目标: G-1
- 关联设计: 窗格渲染器

#### R-01-006 当前会话标识
用户应能从窗格中看出当前正在查看的会话。
- AC-01 当用户切换当前会话时，系统应当高亮窗格中对应的卡片。
- 关联目标: G-1
- 关联设计: 窗格渲染器

#### R-01-007 桌面贴边布局
在桌面宽度下，窗格应当作为主会话左侧的贴边列存在，与外壳侧栏对齐且不遮蔽主会话。
- AC-01 当视口宽度大于移动断点时，系统应当把窗格显示为主会话左侧的贴边列。
- AC-02 当窗格显示时，系统应当不阻止用户正常阅读与操作主会话内容。
- 关联目标: G-3
- 关联设计: 窗格渲染器

#### R-01-008 移动端抽屉与开关
在移动端宽度下，窗格应当默认为隐藏的抽屉，用户可经可见开关展开、经标题行或点击抽屉外部区域收起，且不影响当前会话内容显示。
- AC-01 当视口宽度小于等于移动断点时，系统应当默认隐藏窗格。
- AC-02 当用户点击开关时，系统应当展开窗格；当抽屉处于打开状态时，用户点击或键盘激活「活动会话」标题行任意位置，系统应当收起抽屉。
- AC-03 当视口宽度小于等于移动断点且抽屉处于打开状态时，用户点击或触摸抽屉外部区域，系统应当收起抽屉。
- AC-04 当视口宽度小于等于移动断点时，浮动开关应当显示在会话头部左上角、左边栏切换按钮的右侧，文案为「活动」，不遮挡会话内容与其它界面控件。
- AC-05 当抽屉处于打开状态时，系统应当隐藏浮动开关；抽屉关闭后应当恢复显示。
- AC-06 当视口宽度小于等于移动断点、抽屉处于打开状态且用户点击或键盘激活当前会话对应的卡片时，系统应当收起抽屉，且不发起会话切换。
- 关联目标: G-3
- 关联设计: 窗格渲染器

#### R-01-009 运行状态呈现
运行中的活动卡片应展示会话当前的动态——进行中的工具调用或正在流式的回复，附已运行时长，并给出工具参数摘要、输入/输出 token 计数、输出速率与缓存命中率、回合进度，最近工作项轨迹由工作项时间线承载（见 R-01-012），让用户不必打开会话即可了解每个会话正在干什么、进行到哪一步；卡片不再单独渲染「思考中」「回答中」等当前动作前缀。
- AC-01 当运行中会话存在进行中的工具调用时，系统应当在其卡片的工作项时间线中经执行中的分组行实时呈现该调用的动作摘要。
- AC-02 当运行中会话正在流式回复时，系统应当在其卡片的工作项时间线中实时更新所在分组行或正文行的内容，而不是新增独立的当前动作文案行。
- AC-03 当工具调用结束、流式变化或进入新一轮时，系统应当更新卡片上的工作项时间线与运行统计。
- AC-04 当运行中会话存在进行中的工具调用时，系统应当在其卡片上显示与主会话窗口工作项行一致语义的动作摘要：按工具类型取 description、command、路径、query 等参数的首行（可包含原始命令），无命中时取首个字符串参数值兜底。
- AC-05 当运行中会话的卡片底部进度条下方显示统计行时，系统应当在该行左侧依次显示 tok/s 输出速率（不携带近似符号）、缓存命中率（百分比）、计费输入 token 数与输出 token 数，相邻字段以小圆点区隔；本回合已运行时长应当固定显示于该行最右侧。
- AC-06 当运行中会话的本回合已耗时推进时，系统应当在其卡片上更新回合进度（百分比与进度条），进度由本回合已耗时按 y = t/(t+k) 映射（t 为已耗秒数，y ∈ [0,1)）；半衰期 k 按该会话实测输出速率 r（tok/s，全会话累计口径）校准为 clamp(120×90÷r, 60, 600) 秒、无可用速率时取 120 秒、同一锚点期间冻结不变；同回合内进度单调不减；委托周期内进度按同一锚点连续推进，不因自身回合结束或新回合开始而归零，锚点为该会话最近已知回合起点（无已知起点时以进入委托周期时刻为起点）；委托周期外的回合切换归零重计并按最新实测速率重新校准 k。
- AC-07 当运行中会话显示工作项时间线时，系统应当为其中每个显示行显示聚合状态；显示行不显示行级耗时，与主会话窗口工作项行保持一致；工具项摘要与主会话窗口工作项行的摘要语义一致。
- AC-08 当会话处于运行中状态（含委托周期内保持运行呈现的母会话）时，系统应当令其卡片上的进度条填充持续呈现向右滚动的条纹动画，作为会话活动标志；会话不再运行（等待用户行动、完成提醒或转为非活动）时不呈现条纹（外观对齐 answer-pet）。
- AC-09 当运行中会话显示工作项时间线时，各显示行应当以竖线串起的 5px 圆点时间线呈现，圆点与竖线保持同圆心，且正在执行中的显示行圆点应当带半透明外环并闪烁（外观对齐 answer-pet）。
- AC-10 当运行中会话的工作项时间线不存在正在执行的显示行，且尾部显示行为已定案的非用户输入行时，系统应当将该尾部显示行呈现为执行中状态（圆点蓝色外环闪烁），作为 agent 正在工作的持续标志；会话等待用户行动、不在运行中，或尾部显示行为错误、停止或用户输入行时，不适用该提升。
- 关联目标: G-1
- 关联设计: 活动状态模型

#### R-01-012 活动卡会话上下文与工作项一致性
用户查看活动卡片时，应能直接看到与主会话窗口一致的模型上下文和按折叠分组呈现的最近工作过程，以便在不切换会话的情况下判断工作内容。
- AC-01 当活动卡片显示时，系统应当在右上角显示当前会话主窗口使用的模型名称与 reasoning level；无值时该区域应当为空，不以错误的 preset 名称冒充模型。
- AC-02 当主会话窗口存在工作项时，系统应当按其实际显示顺序以折叠分组呈现展示最近 4 个显示行；不足 4 个时应当全部展示，并包含当前执行中的分组。
- AC-03 当折叠分组行显示在活动卡片中时，组标题、组摘要与聚合状态应当符合 R-01-017 的分组派生规则；当前执行中的分组收到原生推送更新时，卡片应当同步更新。
- AC-04 当活动卡片显示模型信息与工作项时，系统不应当在两者之间渲染独立的「思考中」「回答中」等当前动作文本行。
- AC-05 当工作项表示用户输入消息时，系统应当以人物图标与「用户」标签呈现该行，作为用户语义标识。
- AC-06 当分组内存在执行出错的工作项时，系统应当将该分组行的图标、组标题与摘要整体显示为错误红色。
- AC-07 当显示行同时存在动作文字与后续摘要时，系统应当在两者之间显示一个小圆点分隔符。
- AC-08 当折叠分组包含 Bash 动作时，系统应当在卡片中稳定显示与主会话网页一致的命令图标，不因成员状态或展开状态切换为下拉箭头或其它图标。
- AC-09 当工作项时间线显示 agent 正文输出行时，系统应当以机器人图标与「助手」标签呈现该行；机器人图标与最近卡 agent 角色标识（R-01-013/AC-08）一致。
- AC-10 当工作项时间线涉及 agent 思考语义时，系统应当统一使用「思考」作为思考标签，时间线数据层不得残留英文「Assistant」「Think」标签。
- AC-11 当工作项时间线或最近卡显示机器人图标时，系统应当以与其它图标相同的 12px 字形盒渲染并保持显示尺度一致；图标盒在 14px 行内应当整数像素对齐，描边渲染宽度与 canonical 填充轮廓视觉重量相当，不因 viewBox 留白显小或半像素偏移发虚。
- AC-12 当时间线最近显示行窗口之前存在用户输入行时，系统应当将其中最近一条非空文本行作为指令锚行固定显示在时间线首行，其人物图标、「用户」标签、文字与下划线样式与普通用户输入行一致；指令锚行出现时窗口收缩为最近 3 个显示行，时间线总行数（含锚行）不超过 4。
- AC-13 当最近的用户输入行仍在时间线最近显示行窗口内时，系统不应当显示指令锚行。
- AC-14 当新工作项进入时间线而已有指令锚行显示时，系统应当保持该锚行的位置与内容不变，直到更近的用户输入行按 AC-15 的顶替规则取代它成为新的指令锚行。
- AC-15 当更近的非空文本用户输入行随新工作项上移而到达收缩窗口首行、且时间线首行仍为旧的指令锚行时，系统应当令该用户输入行直接顶替指令锚行（旧锚行消失、其后各行上移一位），此时时间线总行数暂为 3；后续新工作项继续回填，总行数始终不超过 4。
- AC-16 当卡片已显示模型上下文的会话在显示期间切换模型选择时，系统应当将卡片模型上下文更新为切换后的当前选择，不停留在切换前的取值；实时来源（模型目录服务）不可用时保持一次性读取的既有行为。
- 关联目标: G-1
- 关联设计: 活动状态模型

#### R-01-013 最近卡消息预览
用户查看最近历史卡片时，应能用固定的信息层次识别会话与最近交互，而无需打开完整正文。
- AC-01 当最近卡显示时，系统应当在第一行显示工作区名称与模型名称/reasoning level；缺失值应当显示为空。
- AC-02 当最近卡显示时，系统应当在第二行显示会话标题。
- AC-03 当最近卡存在用户消息时，系统应当在第三行显示最近用户消息的第一个非空物理行，并按现有卡片宽度截断；没有内容时该行文本为空。
- AC-04 当最近卡存在 agent reply 时，系统应当在第四行显示最近 agent reply 的第一个非空物理行，并按现有卡片宽度截断；没有内容时该行文本为空。
- AC-05 当最近卡显示活动时间时，系统应当在第五行沿用现有时间格式与时区显示最后活动时间。
- AC-06 当鼠标悬停最近卡时，系统应当沿用现有卡片样例的高亮反馈。
- AC-07 当最近卡显示时，系统应当在第三行文本前常驻显示人物图标与「用户」标签作为用户角色标识，与工作项时间线的用户行形式一致。
- AC-08 当最近卡显示时，系统应当在第四行文本前常驻显示机器人图标与「助手」标签作为 agent 角色标识，与工作项时间线的助手行形式一致。
- AC-09 当最近卡显示时，系统应当以常规字重呈现会话标题，不加粗。
- AC-10 当最近卡显示时，系统应当以低于活动卡的整体不透明度呈现该卡片，弱化历史区视觉强调；悬停时保持既有高亮反馈。
- AC-11 当最近卡在深色或浅色主题下显示时，系统应当以与窗格底色可分辨的卡片底色与可见描边呈现卡片边界，弱化呈现不得使卡片轮廓不可辨。
- 关联目标: G-1
- 关联设计: 窗格渲染器

#### R-01-010 分区与最近会话
窗格应分为上「活动会话」与下「最近历史」两个区域；会话由活动转为非活动后进入最近历史区，让用户既看到当前要处理的会话，也能快速回到最近用过的工作。
- AC-01 当存在当前非活动、且最后活动时间落在最近二十四小时内的会话时，系统应当在窗格下方的最近历史区显示其卡片。
- AC-02 当会话由活动转为非活动时，系统应当将其从活动区移除并放入最近历史区。
- AC-03 当最近历史区呈现多个会话时，系统应当按最后活动时间从新到旧排列。
- AC-04 当活动区没有任何活动会话时，系统应当在活动区显示明确的空态提示而不是空白。
- AC-05 当最近历史区显示时，系统应当在两区分隔线上下各保留 10px 留白。
- AC-06 当主会话结束一轮、且该会话仍为当前会话时（含完成提醒卡被激活后保持焦点的情形），系统应当以"需要响应"标识将其保留在活动区，不移入最近历史区；当该会话存在活动后代时，"需要响应"标识与等待呈现暂不生效，后代全部结束后恢复。
- AC-07 当会话在活动区与最近历史区之间迁移（由活动转为非活动进入历史区，或由非活动转为活动回到活动区）时，系统应当以可感知卡片来源与去向的移动动画呈现该迁移过程。
- AC-08 当会话存在已完成的回合时，系统应当以最后一轮回复结束时刻与宿主列表时间的较新者作为其最后活动时间；当会话尚无已完成回合时，系统应当以宿主列表时间作为其最后活动时间。
- AC-09 当冷会话的回复结束时刻数据在途时，系统应当先以宿主列表时间参与入区判定、排序与时间显示，数据到达后改用精化值重新排序与显示。
- AC-10 当卡片跨区迁移导致同区或另一区其它卡片位置变化时，系统应当使所有位置受影响的卡片以移动动画平滑过渡到新位置，而非瞬间跳变。
- 关联目标: G-1
- 关联设计: 活动状态模型

#### R-01-011 桌面列折叠
桌面形态的窗格应以「活动会话」标题行整体作为收起与展开控件，使用户无需寻找独立按钮即可往返切换，按需给主会话让出宽度。
- AC-03 当视口宽度大于移动断点且窗格处于展开态时，系统应当将「活动会话」标题行整体作为可激活的收起控件，激活后把窗格折叠为不占用主会话宽度的窄条。
- AC-04 当窗格处于折叠窄条状态时，系统应当将窄条整体（含竖排标题与活动计数）作为可激活的展开控件，激活后展开并恢复窗格显示。
- AC-05 当窗格在展开态与折叠态之间切换时，系统应当使该控件保持在窗格顶部的同一屏幕位置。
- AC-06 当视口宽度小于等于移动断点时，系统应当将标题行激活解释为收起抽屉（与桌面同一交互路径），而不是折叠为窄条。
- 关联目标: G-3
- 关联设计: 窗格渲染器

#### R-01-015 桌面拖拽调宽
用户在桌面形态下可拖拽窗格右缘调整窗格列宽，以便按信息密度偏好分配窗格与主会话的横向空间；宽度被夹取在允许范围内，折叠窄条与移动端抽屉不受影响，调整结果在刷新后恢复。
- AC-01 当用户在桌面形态下拖拽窗格右缘手柄时，系统应当实时调整窗格宽度并使主会话内容同步让位。
- AC-02 当拖拽目标位置超出允许范围时，系统应当将宽度夹取在最小 200px 与最大 480px 之间。
- AC-03 当窗格处于折叠窄条状态或移动端抽屉形态时，系统应当不提供拖拽调宽。
- AC-04 当用户调整宽度后刷新页面时，系统应当恢复上次调整的宽度。
- 关联目标: G-3
- 关联设计: 窗格渲染器

#### R-01-018 回到顶部
用户在窗格内向下滚动浏览后，应能一键回到窗格顶部，而不必手动长距离回滚。
- AC-01 当窗格滚动位置离开顶部超过阈值时，系统应当在窗格右下角显示悬浮的「回到顶部」图标按钮；未超过阈值时不显示。
- AC-02 当用户点击或键盘激活「回到顶部」图标按钮时，系统应当将窗格内容滚动回顶部；系统开启减弱动态效果时应当直接定位，不做平滑滚动。
- AC-03 当窗格滚动位置回到阈值以内时，系统应当隐藏「回到顶部」图标按钮。
- AC-04 当窗格处于移动端抽屉形态时，系统应当同样在抽屉内提供「回到顶部」图标按钮；窗格折叠为桌面窄条时不显示。
- AC-05 当「回到顶部」按钮显示时，系统应当以纯图标（无文字）与不透明底色呈现，并提供可访问名称。
- 关联目标: G-3
- 关联设计: 窗格渲染器
#### R-01-014 加载过程可见与渐进呈现
用户打开或刷新 Web GUI 后，应能分辨「数据正在加载」与「确实没有数据」，且各部分数据就绪即出现，不互相等待，也不妨碍窗格既有交互。
- AC-01 当会话列表尚未就绪时，系统应当在活动区与最近历史区分别显示加载中活动指示，而不是显示空态提示。
- AC-02 当卡片补充数据（模型、工作项时间线、消息预览）在途时，系统应当在卡片对应位置显示加载指示；数据返回后就地填充，不重建卡片。
- AC-03 当部分卡片数据先就绪时，系统应当先显示已就绪内容，不等待其余卡片。
- AC-04 当数据加载尚未完成时，系统应当保持窗格全部既有交互（滚动、折叠、抽屉开关、卡片激活）可用。
- AC-05 当补充数据加载失败时，系统应当降级为空字段，并允许该会话再次可见时重试，而不是永久空白。
- AC-06 当会话列表尚未就绪时，系统应当将活动数量标识显示为加载指示而非「0/0」等具体计数；列表就绪后显示实际计数。
- 关联目标: G-1
- 关联设计: 窗格渲染器

#### R-01-016 等待卡近期轨迹
用户查看活动区中等待其行动的卡片时，应能直接看到该会话最近的工作项轨迹，让用户不必打开会话即可了解这些会话近期做了什么。
- AC-01 当等待卡（需要响应、待确认、待审查、待回复）显示时，系统应当在其卡片上显示该会话最近的工作项时间线（最多 4 个显示行，图标、文字、状态语义与运行卡时间线一致）。
- AC-04 当上述卡片的时间线数据在途时，系统应当在时间线区域显示加载指示，数据返回后就地填充（沿用 R-01-014 的字段级加载语义）。
- 关联目标: G-1
- 关联设计: 活动状态模型
#### R-01-017 折叠时间线
活动卡的工作项时间线一律采用折叠分组呈现——相邻工作过程合并成组、只显示组标题与关键内容——使窗格与主会话折叠视图的信息密度一致；该呈现是窗格自有的数据层派生，不依赖 dsh-auto-collapse 插件存在。
- AC-01 当 dsh-auto-collapse 未安装或未生效时，系统应当仍以折叠分组呈现工作项时间线，且窗格加载与全部功能不因该插件缺失而降级或报错；该插件存在并生效时呈现保持一致。
- AC-02 当折叠分组呈现时，系统应当把用户输入项或正文工作项之间的连续工具调用与思考工作项合并为一个分组行；分组不得跨越用户输入项或正文工作项。
- AC-03 当分组内存在执行中的工具调用时，系统应当以「正在运行」为组标题并实时显示该成员摘要；否则当分组内存在执行中的思考时以「正在思考」为组标题并实时显示其最新行；否则按组内构成显示「运行了命令」「编辑了文件」「上下文注入」或「已思考」组标题。
- AC-04 当分组包含思考工作项且组内无执行中的工具调用时，系统应当在组摘要中显示推理输出的文本内容，流式期间跟随最新行。
- AC-05 当分组包含多个成员时，系统应当聚合成员状态：任一成员执行中为执行中；否则含错误为错误；否则含中断为中断；否则为已完成；R-01-009/AC-10 的尾部提升规则在折叠呈现下同样适用于分组行。
- AC-06 当折叠呈现显示时间线时，系统应当最多显示最近 4 个分组行，顺序与主会话窗口一致；等待卡同样适用折叠分组语义；存在指令锚行（R-01-012/AC-12）时窗口收缩为最近 3 个分组行，时间线总行数（含锚行）不超过 4。
- 关联目标: G-1
- 关联设计: 活动状态模型

#### R-02-001 独立数据源
插件应只依赖 DSH 原生会话与工作区服务提供活动数据，不依赖第三方宠物插件及其数据路由，也不主动轮询状态。
- AC-01 当未安装任何第三方宠物插件时，插件应当仍能正常取得会话与等待行动状态并渲染窗格。
- AC-02 当插件单次重绘活动概览时，系统应当只依赖宿主会话与工作区服务的订阅推送，而不依赖任何第三方插件的 HTTP 状态路由。
- 关联目标: G-2
- 关联设计: 活动状态模型

#### R-02-002 外壳重挂载健壮性
外壳应用重挂载（布局重建、导航、会话切换）时窗格应自动恢复且不重复。
- AC-01 当框架重建导致已挂载窗格被移除时，系统应当自动恢复窗格，且不产生重复实例。
- AC-02 当窗格的宿主布局尚未出现时，系统应当保持静默等待，不向控制台报错。
- 关联目标: G-3
- 关联设计: 窗格渲染器

#### R-02-003 渲染稳定与资源释放
渲染应避免无谓的 DOM 写入，插件卸载时应完整清理其注入的元素、样式与事件监听。
- AC-01 当状态未发生变化而重复渲染时，系统应当跳过全部可见 DOM 写入。
- AC-02 当插件被卸载时，系统应当移除其注入的元素、样式与事件监听。
- 关联目标: G-2
- 关联设计: 活动状态模型

#### R-02-004 轮内状态订阅纪律
运行状态的实时更新应来自对运行中会话的原生订阅推送，且订阅生命周期可逆，不引入新的轮询。
- AC-01 当会话仍在运行时，系统应当保持对其轮内状态的订阅；当会话停止运行或插件卸载时，系统应当断开相应订阅。
- AC-02 当插件需要轮内状态时，系统应当只从对运行中会话的原生订阅推送取得，而不引入新的 HTTP 状态轮询。
- 关联目标: G-2
- 关联设计: 窗格渲染器
