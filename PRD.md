---
doc-type: prd
mutation: living
id-prefix: R
owner: 项目属主
---

# PRD — 活动会话总览窗格

## 项目愿景

在 DeepSeek Harness Web 中，把分散在侧栏各工作区里的"正在运行"与"完成一轮后等待你行动"的会话收拢到一个可贴边、可滚动的窗格里，让用户一屏掌握所有需要关注的会话，并可直接跳转，取代向上溢出的浮动会话面板与遮挡主会话的移动端浮层。

## 目标

- **G-1** 用户无需逐会话翻看即可掌握所有运行中与等待行动的会话，并可直达目标会话。
- **G-2** 插件仅依赖 DSH 原生会话与工作区服务，可脱离任何第三方宠物插件独立运行，卸载可逆。
- **G-3** 窗格的桌面与移动形态保证可用：独立滚动、不遮挡主会话、外壳重挂载后自恢复。

## 非目标

- **NG-1** 渲染会话完整正文与完整历史；窗格只展示有限的工作项与消息首行预览，并提供跳转。
- **NG-2** 在本初版内提供会话语义操作（重命名、取消、队列编辑、直接响应等待项）。
- **NG-3** 桌面宠物、主题动画等装饰性呈现。
- **NG-4** 复用或代理第三方插件的状态路由与轮询协议。

## 需求清单

#### R-01-001 活动会话总览
用户浏览 Web GUI 时可在窗格中一次看到所有正在运行、以及完成一轮后等待其行动的会话，并获知工作区归属与当前会话位置。
- AC-01 当存在正在运行或等待用户行动的会话时，系统应当逐一显示对应的活动卡片。
- AC-02 当没有任何活动会话时，系统应当显示明确的空态，而不是报错或空白。
- AC-03 当会话状态发生变化（开始运行、结束、出现等待行动、等待解除）时，系统应当自动更新窗格内容，无需用户手动刷新。
- 关联目标: G-1
- 关联设计: 活动状态模型

#### R-01-002 等待行动可见性
完成一轮或正在等待用户输入的会话应当被醒目标识，并说明等待类型，方便用户决定先行处理哪个会话。
- AC-01 当会话等待用户确认（待确认）时，系统应当显示"待确认"标识。
- AC-02 当会话等待用户审查或回复时，系统应当分别显示"待审查"或"待回复"标识。
- AC-03 当主会话完成一轮而尚未被打开时，系统应当以"需要响应"标识并采用区别于运行中的呈现样式。
- 关联目标: G-1
- 关联设计: 活动状态模型

#### R-01-003 层级与归属
用户应当能从窗格卡片分辨会话的层级关系（母会话与子代理）与所属工作区。
- AC-01 当会话存在直属子代理时，系统应当以嵌套缩进把子代理卡片显示在其母会话卡片之下。
- AC-02 当子代理会话结束时，系统应当不再显示该子代理卡片。
- AC-03 当主会话可归属到某个工作区时，系统应当在其卡片上显示该工作区名称。
- 关联目标: G-1
- 关联设计: 活动状态模型

#### R-01-004 数量规模与滚动
当活动会话数量较多时，窗格应当保持可读、可滚动，不把卡片顶出屏幕，也不遮挡主会话内容。
- AC-01 当活动卡片总高度超过窗格可视区域时，系统应当允许用户在窗格内滚动查看全部卡片。
- AC-02 当用户在窗格内滚动时，系统应当保持主会话内容的滚动位置不变。
- 关联目标: G-1
- 关联设计: 窗格渲染器

#### R-01-005 跳转到会话
用户激活某张活动卡片后应当切换到对应的会话。
- AC-01 当用户点击或键盘激活一张活动卡片时，系统应当将当前会话切换为该卡片对应的会话。
- AC-02 当所点卡片对应的会话尚未进入客户端会话列表时，系统应当等待列表就绪后完成跳转；当等待超过固定时限仍未就绪时，系统应当结束本次交互，且不影响其它卡片。
- 关联目标: G-1
- 关联设计: 窗格渲染器

#### R-01-006 当前会话标识
用户应能从窗格中看出当前正在查看的会话。
- AC-01 当用户切换当前会话时，系统应当高亮窗格中对应的卡片。
- 关联目标: G-1
- 关联设计: 窗格渲染器

#### R-01-007 桌面贴边布局
在桌面宽度下，窗格应当作为主会话左侧的贴边列存在，与外壳侧栏对齐且不遮蔽主会话。
- AC-01 当视口宽度大于移动断点时，系统应当把窗格显示为主会话左侧的贴边列。
- AC-02 当窗格显示时，系统应当不阻止用户正常阅读与操作主会话内容。
- 关联目标: G-3
- 关联设计: 窗格渲染器

#### R-01-008 移动端抽屉与开关
在移动端宽度下，窗格应当默认为隐藏的抽屉，用户可经可见开关或点击抽屉外部区域展开与收起，且不影响当前会话内容显示。
- AC-01 当视口宽度小于等于移动断点时，系统应当默认隐藏窗格。
- AC-02 当用户点击开关时，系统应当展开窗格；再次点击应收起。
- AC-03 当视口宽度小于等于移动断点且抽屉处于打开状态时，用户点击或触摸抽屉外部区域，系统应当收起抽屉（不影响浮动开关按钮自身的切换）。
- 关联目标: G-3
- 关联设计: 窗格渲染器

#### R-01-009 运行状态呈现
运行中的活动卡片应展示会话当前的动态——进行中的工具调用或正在流式的回复，附已运行时长，并给出工具参数摘要、输出 token 计数与速率、阶段进度与最近流程节点轨迹，让用户不必打开会话即可了解每个会话正在干什么、进行到哪一步；卡片不再单独渲染「思考中」「回答中」等当前动作前缀。
- AC-01 当运行中会话存在进行中的工具调用时，系统应当在其卡片的工作项时间线中显示该调用的工具名称。
- AC-02 当运行中会话正在流式回复时，系统应当在其卡片的工作项时间线中实时更新当前回复项，而不是新增独立的当前动作文案行。
- AC-03 当工具调用结束、流式变化或进入新一轮时，系统应当更新卡片上的工作项时间线与运行统计。
- AC-04 当运行中会话存在进行中的工具调用时，系统应当在其卡片上显示基于该工具参数白名单字段的摘要；摘要不得包含完整命令或原始参数 JSON。
- AC-05 当运行中会话正在生成输出时，系统应当在卡片底部进度条下方显示输出 token 计数与输出速率（累计口径的近似值即可）。
- AC-06 当运行中会话的回合阶段推进时，系统应当在其卡片上更新阶段进度（百分比与进度条）；同回合内进度不得倒退，回合切换时重置。
- AC-07 当运行中会话存在流程节点（阶段或工具）时，系统应当在其卡片上显示最近少量节点的轨迹，含节点状态与耗时；节点文案不得泄露敏感参数。
- AC-08 当运行中会话处于流式回复阶段时，系统应当令其卡片上的进度条内部呈现向右滚动的条纹/阴影动画；进入工具或思考阶段后条纹停止（外观对齐 answer-pet）。
- AC-09 当运行中会话显示流程节点轨迹时，节点应当以竖线串起的圆点时间线呈现，且正在执行的节点圆点应当带半透明外环并闪烁（外观对齐 answer-pet）。
- 关联目标: G-1
- 关联设计: 活动状态模型

#### R-01-012 活动卡会话上下文与工作项一致性
用户查看活动卡片时，应能直接看到与主会话窗口相同的模型上下文和最近工作项，以便在不切换会话的情况下判断工作内容。
- AC-01 当活动卡片显示时，系统应当在右上角显示当前会话主窗口使用的模型名称与 reasoning level；无值时该区域应当为空，不以错误的 preset 名称冒充模型。
- AC-02 当主会话窗口存在工作项时，系统应当按其实际显示顺序展示最近 4 个工作项；不足 4 个时应当全部展示，并包含当前执行项。
- AC-03 当工作项显示在活动卡片中时，系统应当保留主会话窗口对应的图标语义、文字、详情、状态与顺序；当前执行项收到原生推送更新时，卡片应当同步更新。
- AC-04 当活动卡片显示模型信息与工作项时，系统不应当在两者之间渲染独立的「思考中」「回答中」等当前动作文本行。
- AC-05 当工作项表示用户输入消息时，系统应当显示小人物图标作为用户语义标识。
- AC-06 当工具工作项执行出错时，系统应当保留该动作原有图标，并将图标、动作文字与摘要直接显示为错误红色。
- AC-07 当工作项同时存在动作文字与后续摘要时，系统应当在两者之间显示一个小圆点分隔符。
- AC-08 当工作项为 Bash 动作时，系统应当在卡片中稳定显示与主会话网页一致的 Bash 图标，不因工作项状态或展开状态切换为下拉箭头或其它图标。
- 关联目标: G-1
- 关联设计: 活动状态模型

#### R-01-013 最近卡消息预览
用户查看最近历史卡片时，应能用固定的信息层次识别会话与最近交互，而无需打开完整正文。
- AC-01 当最近卡显示时，系统应当在第一行显示工作区名称与模型名称/reasoning level；缺失值应当显示为空。
- AC-02 当最近卡显示时，系统应当在第二行显示会话标题。
- AC-03 当最近卡存在用户消息时，系统应当在第三行显示最近用户消息的第一个非空物理行，并按现有卡片宽度截断；没有内容时应当为空。
- AC-04 当最近卡存在 agent reply 时，系统应当在第四行显示最近 agent reply 的第一个非空物理行，并按现有卡片宽度截断；没有内容时应当为空。
- AC-05 当最近卡显示活动时间时，系统应当在第五行沿用现有时间格式与时区显示最后活动时间。
- AC-06 当鼠标悬停最近卡时，系统应当沿用现有卡片样例的高亮反馈。
- 关联目标: G-1
- 关联设计: 窗格渲染器

#### R-01-010 分区与最近会话
窗格应分为上「活动会话」与下「最近历史」两个区域；会话由活动转为非活动后进入最近历史区，让用户既看到当前要处理的会话，也能快速回到最近用过的工作。
- AC-01 当存在当前非活动、且最近二十四小时内最后一次活动过的会话时，系统应当在窗格下方的最近历史区显示其卡片。
- AC-02 当会话由活动转为非活动时，系统应当将其从活动区移除并放入最近历史区。
- AC-03 当最近历史区呈现多个会话时，系统应当按最后一次活动的时间从新到旧排列。
- AC-04 当活动区没有任何活动会话时，系统应当在活动区显示明确的空态提示而不是空白。
- 关联目标: G-1
- 关联设计: 活动状态模型

#### R-01-011 桌面列折叠
桌面形态的窗格应提供收起与展开控制，让用户按需给主会话让出宽度。
- AC-01 当用户点击桌面列的收起控制时，系统应当把窗格折叠为不占用主会话宽度的窄条。
- AC-02 当用户单击折叠后的窄条时，系统应当展开并恢复窗格显示。
- 关联目标: G-3
- 关联设计: 窗格渲染器

#### R-02-001 独立数据源
插件应只依赖 DSH 原生会话与工作区服务提供活动数据，不依赖第三方宠物插件及其数据路由，也不主动轮询状态。
- AC-01 当未安装任何第三方宠物插件时，插件应当仍能正常取得会话与等待行动状态并渲染窗格。
- AC-02 当插件单次重绘活动概览时，系统应当只依赖宿主会话与工作区服务的订阅推送，而不依赖任何第三方插件的 HTTP 状态路由。
- 关联目标: G-2
- 关联设计: 活动状态模型

#### R-02-002 外壳重挂载健壮性
外壳应用重挂载（布局重建、导航、会话切换）时窗格应自动恢复且不重复。
- AC-01 当框架重建导致已挂载窗格被移除时，系统应当自动恢复窗格，且不产生重复实例。
- AC-02 当窗格的宿主布局尚未出现时，系统应当保持静默等待，不向控制台报错。
- 关联目标: G-3
- 关联设计: 窗格渲染器

#### R-02-003 渲染稳定与资源释放
渲染应避免无谓的 DOM 写入，插件卸载时应完整清理其注入的元素、样式与事件监听。
- AC-01 当状态未发生变化而重复渲染时，系统应当跳过全部可见 DOM 写入。
- AC-02 当插件被卸载时，系统应当移除其注入的元素、样式与事件监听。
- 关联目标: G-2
- 关联设计: 活动状态模型

#### R-02-004 轮内状态订阅纪律
运行状态的实时更新应来自对运行中会话的原生订阅推送，且订阅生命周期可逆，不引入新的轮询。
- AC-01 当会话仍在运行时，系统应当保持对其轮内状态的订阅；当会话停止运行或插件卸载时，系统应当断开相应订阅。
- AC-02 当插件需要轮内状态时，系统应当只从对运行中会话的原生订阅推送取得，而不引入新的 HTTP 状态轮询。
- 关联目标: G-2
- 关联设计: 窗格渲染器
