---
doc-type: domain
mutation: living
owner: 双方
---

# DOMAIN — 领域词典

## 术语

- **会话**: 宿主中一次可寻址的 agent 工作单位，具有稳定 id、显示标题与状态。
- **主会话**: 没有父级会话归属的会话。
- **母会话**: 直接拥有一个或多个子代理的会话；可以是主会话，也可以是更深层子代理。
- **子代理**: 作为另一会话直属子级运行的会话；结束后即离开活动视图。
- **工作区**: 会话所属的工作目录分组；窗格以名称徽标形式展示。未归入任何工作区的会话没有徽标。
- **工作区色相**: 由工作区身份（路径优先、名称兜底）派生稳定基色，并在同一可见工作区集合内确定性映射到七个避红 OKLCH 感知锚点；同一工作区的全部徽标恒为同一色相，用于工作区名称徽标的图标、文字、底色与描边着色；撞槽身份跨色区分配，不使用红色系。
- **层级连接线**: 活动区中位于母会话与子代理卡片缩进槽内、用于表达直属层级关系的视觉线段。
- **当前会话**: 用户当前正在查看的会话；窗格以高亮标识。
- **活动会话**: 正在运行、或正在等待用户行动（阻塞等待、完成提醒或错误提醒）、或存在活动后代的会话；出现在窗格顶部「活动会话」区。
- **委托周期**: 自会话出现活动后代起、至后代全部结束且处理其结果的回合完成止的连续时间段；期间母会话卡片保持运行中呈现、进度按同一锚点连续推进，完成提醒暂不生效。
- **最近会话**: 当前非活动、仍被会话服务列出的**主会话**（不含子代理）；按最后活动时间从新到旧出现在窗格下方「最近历史」区。
- **活动区**: 窗格上部，仅承载活动卡片的区域。
- **历史区**: 窗格下部，承载最近会话卡片的区域；历史卡片按最后活动时间从新到旧分批呈现。
- **历史分页**: 历史区按排序结果划分的显示批次；每批最多 10 条，已呈现批次保留，滚动到底部后追加下一批，直至没有更多最近会话。
- **宿主列表时间**: 宿主 `sessions.list` 条目的 `updatedAt`——会话创建时刻与最后一条真人用户消息时刻中的较新者。
- **最后活动时间**: 会话最后一轮回复结束时刻与**宿主列表时间**中的较新者；无已完成回合时即宿主列表时间；冷会话回复结束时刻在途时先以宿主列表时间参与历史区判定、排序与显示，数据到达后取较新者精化。
- **活动卡片**: 窗格中表示一个活动会话的条目。
- **最近卡片**: 窗格中表示一个最近会话的条目。
- **轮内状态**: 会话单个回合进行中的状态——正在调用的工具名、是否正在流式或推理回复、输出 token 计数与近似输出速率、已运行时长、工作项时间线与阶段进度；由对运行中会话的原生订阅推送与列表快照投影提供（时长和进度在渲染期按回合开始时间实时计算）。
- **最近回合耗时**: 最近一个已结束回合的起止时刻差值；在等待行动卡上固定显示，不包含回合结束后的等待时间，缺少任一起止时刻时为空。
- **输出速率**: 会话生成输出的速率（token/秒），由累计输出 token 与耗时派生；在卡片上以「≈」前缀标注为近似值。
- **轮内进度**: 运行卡对回合推进程度的百分比估计，由已耗时按 y = t/(t+k) 映射；半衰期 k 按会话最新实测累计输出速率校准（基准速率 90 tok/s 对应 120s，夹取 [60, 600]s，无可用速率起步取 540s 即 20 tok/s 最慢基准）；每次进度更新按最新实测速率重校准，进度是对完成度的实时估计、允许随速率回落而回退（不承诺单调不减）；委托周期内按同一锚点连续推进，委托周期外回合切换归零重计。
- **加载指示**: 数据在途时在区域或卡片字段位置显示的活动图标（spinner），表示该处正在拉取数据；与空态互斥——会话列表未就绪时显示加载指示而非空态。
- **工作项**: 主会话窗口中按显示顺序出现的一个可识别工作单元，包含图标语义、文字、详情与状态；活动卡将其折叠为分组行呈现。
- **工作项时间线**: 活动卡中按主会话窗口实际显示顺序派生的工作项序列（以**折叠时间线**呈现）；当前执行工作项由原生会话推送持续更新。
- **当前活动行**: 运行中会话时间线内代表真实在飞 partial、running call 或当前执行分组的显示行；时间线选择必须保留其身份、文字与执行中状态并将最新一条置于末行，仅在不存在当前活动行时才允许以尾部提升充当持续工作标志。
- **工作项分组**: 折叠时间线中的一个显示行，聚合相邻连续工作过程工作项（工具调用、思考或上下文注入）的组标题、摘要与聚合状态。
- **折叠时间线**: 工作项时间线的唯一呈现形态：相邻连续工作过程合并成分组行呈现；分组规则改编自 dsh-auto-collapse 的折叠语义，为窗格自有数据层派生，不依赖该插件存在。
- **模型上下文**: 会话当前主窗口使用的模型名称与 reasoning level；缺失字段保持空值。
- **指令锚行**: 停留于时间线第一行的最近一条非空文本用户输入行；用户输入行作为普通显示行自时间线末尾进入、随新行到达向上滚动，触顶后停留为指令锚行并占用一个总预算名额，其后显示最近最多 3 个工作显示行，时间线总行数不超过 4；更近的非空文本用户输入行滚动至第二行时取代旧锚行。
- **物理首行**: 文本按原始换行拆分后，第一个去除首尾空白且非空的行；不存在时为空。
- **消息预览**: 最近卡分别展示最近用户消息与最近 agent reply 的物理首行，不等同于完整正文。
- **等待行动**: 会话因**阻塞等待**、**完成提醒**或**错误提醒**而需要用户关注的状态。
- **阻塞等待**: 会话因待确认、待审查或待回复而停止不前、必须等用户给出答复才能继续的等待行动状态；卡片以金色卡面催促尽快响应，末行首行为前置类型图标的「待确认 / 待审查 / 提问中」类型胶囊、其后为正文——待确认/待审查为动作说明，待回复为语义化 HTML 提问列表（仅一个可展示问题时使用 bullet list，多个可展示问题时使用编号列表且编号对应原始问题位置；每项显示问题正文物理首行、行尾多余冒号不显示，最多 3 个问题，其后仍有问题时追加省略项，问题正文不可得时回落该条短标题），胶囊与正文同频同相闪烁。
- **完成提醒**: 主会话存在已完成且尚未被确认的回合时的等待行动状态；卡片以绿色成功色卡面呈现（任务成功执行完毕、不必抢答），末行首行为绿色对勾图标的「已完成」类型胶囊、其后第二行左侧为「继续对话，或移入历史」并整行脉冲闪烁、右侧为「移入历史」**确认按钮**；其成立与解除由宿主持久状态承载，不依赖客户端在线观测，页面刷新或重新连接后恢复。
- **错误提醒**: 主会话最近一个回合以不可恢复错误结束（回合结束原因为错误）时的等待行动状态；卡片以红色错误色卡面呈现（会话出错、需要用户注意），末行首行为红色感叹号图标的「错误」类型胶囊、其后显示错误信息正文，胶囊与正文同频同相闪烁；随该会话新回合结束（无论新回合结果）覆盖解除，不提供确认按钮；其成立与解除同样由宿主持久登记承载，页面刷新或重新连接后恢复。
- **完成确认**: 解除完成提醒的显式用户动作：激活**确认按钮**后写回宿主、在所有客户端同步生效；用户发送新指令视为对既有完成的隐式确认，新回合完成后重新触发提醒。打开会话或切换当前会话不构成确认。
- **确认按钮**: 完成提醒卡上的显式确认控件，展示文案为「移入历史」（写回 ack 后卡片迁入最近历史区）；仅完成提醒卡提供，阻塞等待卡与错误提醒卡不提供。
- **活动总览窗格**: 桌面形态下真实占据中间列左侧的贴边列（会话内容随之让位）/ 移动形态下的抽屉；承载活动区与历史区两段列表的容器。
- **窄条**: 桌面列折叠后的形态，不占用主会话宽度，显示活动计数并可点击展开；会话内容随折叠同步让位恢复。
- **抽屉**: 移动端点（窗口宽度 ≤ 767px）下窗格的收纳形态，默认隐藏在屏外，经开关展开与收起。
- **移动断点**: 窗格从"贴边列"切换为"抽屉"的视口宽度阈值（767px）。
- **外壳**: DeepSeek Harness Web 的应用框架；包含 AppFrame 三栏布局与槽座（sidebar、conversation、details 等）。
- **完成确认宿主侧**: 插件在宿主进程内的 cordis 半端：订阅会话事件登记回合结束时刻、持久化确认游标，并经 HTTP/SSE 通道向全部客户端下发与接收确认写回；是**完成确认**状态的唯一持久化归属。
- **活动状态模型**: 把宿主会话快照归一化为活动区/历史区条目与轮内状态文案的领域模块。
- **窗格渲染器**: 负责窗格挂载、双区绘制、滚动、跳转、桌面折叠与移动端抽屉的领域模块。
- **E2E 验证基建**: 仓库级验证设施：mock LLM **E2E 剧本**服务、**隔离测试环境**启动器与 Playwright 浏览器 spec 的集合；只承载验收点的自动化锚定，不承担产品行为（C-045）。
- **隔离测试环境**: 以独立 `$DSH_HOME` 临时目录与 OS 分配空闲端口启动的 dsh web 实例，settings 预置指向 mock LLM，与日常实例互不影响（C-045）。
- **E2E 剧本**: mock LLM 服务按测试场景返回的预编排 SSE 响应序列（慢速流式输出、工具调用、立即结束），由用户消息关键词选择，在浏览器 E2E 中确定性制造会话活动（C-045）。

## 实体与关系

- **母会话**（父）—拥有→**子代理**（子）：由母会话派生，形成层级树。
- **工作区**（组）—包含→**会话**：按会话 cwd 归属；决定主会话卡片在窗格中的顺序。
- **活动状态模型**—消费→**会话**：读取会话服务快照，产出**活动卡片**条目与**最近卡片**条目。
- **活动状态模型**—消费→**轮内状态**：把运行中会话的原生订阅快照归一为运行统计与工作项时间线。
- **活动状态模型**—消费→**模型上下文**与**消息预览**：把 native session snapshot/projection 归一为卡片元数据。
- **窗格渲染器**—承载→**活动卡片**与**最近卡片**：在**活动总览窗格**的活动区与历史区中绘制与交互。
- **活动总览窗格**—形态→**抽屉** 或→**窄条**：随**移动断点**或折叠控制在贴边列、抽屉、窄条之间切换。

## 生命周期

- **活动卡片**的生命随其**会话**的活动性：会话进入"正在运行或等待行动"时出现，主会话完成一轮后进入**完成提醒**——完成未被确认期间以完成提醒呈现并留在活动区，经确认按钮「移入历史」（或新回合隐式更替）后转入历史区；主会话最近回合以不可恢复错误结束后进入**错误提醒**——随新回合结束覆盖解除、无确认按钮；子代理结束后即消失；委托周期中的母会话保持运行中呈现，随后代全部结束且处理其结果的回合完成而退出委托周期。
- **会话在活动区与历史区之间迁移**：当会话不再满足活动判定时转入历史区，重新满足活动判定时回到活动区，均由每次渲染的数据派生完成，渲染层以移动动画呈现迁移过程；迁移导致位置受影响的其它卡片同样以移动动画过渡。
- **轮内状态订阅**：随会话进入运行态建立、随会话停止运行或插件卸载断开；与窗格的渲染生命周期同生共灭。
- **活动总览窗格**随外壳挂载而创建、随外壳重挂载而重建（不重复、自动恢复），随插件卸载而移除。
- 除完成确认按钮的确认写回外，窗格内容不写回宿主：会话状态的变化方向恒为 宿主服务 → 活动卡片。

## 跨模块不变量

- 主会话卡片按所在工作区的侧栏顺序排布；未归入任何工作区的主会话排在所有工作区之后并保持其出现顺序。
- 子代理卡片始终跟随其母会话卡片，以缩进和层级连接线表达直属关系；深度大于其母会话。
- 只要会话存在活动后代，其全部有效母会话都必须在活动区保持运行中呈现，且不得同时进入历史区。
- 等待行动的会话以区别于运行中的样呈现，并给出等待类型文案；**阻塞等待**、**完成提醒**与**错误提醒**的紧迫度以色彩语义一眼区分——阻塞等待为金色卡面、完成提醒为绿色成功色卡面、错误提醒为红色错误色卡面；三类均以末行「类型胶囊 + 正文」的结构呈现并脉冲闪烁提醒用户行动，列头、窄条与移动端「活动」按钮的数量胶囊以相同固定频率与其同步闪烁；标题状态点均不闪烁，卡片描边与光晕强度一致。
- 一个会话要么在活动区、要么在历史区，绝不同时出现于两区。
- 最近会话的判定只依赖会话服务快照中的会话状态与最后活动时间戳，不依赖插件私有记录；历史分页只改变呈现批次，不改变候选集合与排序口径。
- 历史分页按排序结果追加，每个会话最多进入一次历史区，追加到末尾后不再重复加载。
- 轮内状态订阅数量与运行中会话数量一致，停止运行或卸载后订阅归零。
- 活动条目顺序在快照未变化时必须稳定，重复渲染不产生可见变化。
- 除可滚动的窗格列表外，窗格不得修改宿主 DOM 或会话/工作区数据。
- **工作项时间线**必须使用主会话窗口的实际工作顺序；总行数不超过 4，存在**指令锚行**时其后为最近最多 3 个工作显示行；运行中存在**当前活动行**时必须保留其身份、文字与状态并将最新一条置于末行，不得由锚行裁剪或旧尾行冒充。
- **工作项分组**必须以用户输入项与正文工作项为硬边界，不得跨边界合并；窗格功能不得依赖 dsh-auto-collapse 插件存在。
- **指令锚行**停留于时间线首行并占用一个总预算名额：工作显示行名额固定为最多 3 个；更近的非空文本用户输入行滚动至第二行时取代旧锚行、其后各行上移且总行数暂减一；空文本用户输入行不停留不取代；时间线总行数恒不超过 4。
- **模型上下文**不得把 `agentPreset` 当作模型名称；模型或 reasoning level 缺失时保持空白；模型选择在显示期间变更时跟随当前选择更新，不停留旧值。
