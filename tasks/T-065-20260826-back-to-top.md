---
doc-type: task
mutation: lifecycle
id: T-065
---

# T-065 回到顶部悬浮按钮

状态: completed
关联: R-01-018 / 窗格渲染器
风险等级: standard

## 背景与目标

东家需求：窗格滚动后在面板下方显示悬浮的「回到顶部」快捷按钮，一键回到窗格顶部。已升级为 R-01-018（AC-01 阈值显示 / AC-02 激活回顶、reduced-motion 直接定位 / AC-03 回到阈值内隐藏 / AC-04 移动端抽屉同适用、桌面窄条不显示）。

## 差距评估

- `.dap-scroll` 已有滚动监听（`bindPaneControls#onScroll`，R-01-004/AC-03 滚动条显隐），回到顶部按钮的显隐判定可挂入同一监听，无需新增 scroll 监听。
- 窗格骨架（`ensurePane` innerHTML）无按钮元素，需新增 `.dap-top` 胶囊按钮（默认 `hidden`）与配套 CSS。
- 激活回顶经 `scrollTo({ top: 0 })`；`prefersReducedMotion()` 已存在，命中时用 `auto`。

## 收敛方案

- 骨架：`.dap-scroll` 之后新增 `<button class="dap-top" hidden>↑ 回到顶部</button>`，随窗格创建/移除。
- CSS：`.dap-top` 绝对定位于窗格底部居中（`position:absolute; bottom:12px; left:50%; transform:translateX(-50%)`，z-index 高于滚动内容），胶囊样式与窗格主题协调；桌面折叠窄条态 `display:none`。
- JS：`bindPaneControls` 内 onScroll 同步 `topBtn.hidden = scroll.scrollTop <= TOP_THRESHOLD`（阈值常量 200px）；按钮 click 激活 `scroll.scrollTo({ top: 0, behavior: prefersReducedMotion() ? "auto" : "smooth" })`；unbind 清理 click 监听。
- 按钮为原生 `<button>`，键盘聚焦 + Enter/Space 激活由浏览器原生提供（click 同路径）。

## 测试计划

- `scripts/check.mjs`：bundle 契约断言骨架按钮、CSS、显隐判定、激活回顶与 unbind 清理（锚定 R-01-018/AC-01～AC-04、R-02-003/AC-02）。
- `scripts/acceptance.mjs`：新增 R-01-018 人工验收步骤（桌面 + 移动端抽屉 + 窄条 + reduced-motion）。
- `pnpm build:client && pnpm check`；`python3 tools/agentmap_lint.py --report`。
- 独立 Standards/Spec review。

## 验证矩阵

| 维度 | 适用性/理由 | 可执行证据 |
|---|---|---|
| 成功 | 适用：滚动超过阈值显示按钮，激活回顶后隐藏 | `scripts/acceptance.mjs#R-01-018`、`src/client.mjs::bindPaneControls` |
| 异常 | 适用：reduced-motion 下直接定位无平滑滚动 | `scripts/check.mjs#R-01-018/AC-02`、`src/client.mjs::bindPaneControls` |
| 边界配置 | 适用：移动端抽屉同样显示；桌面折叠窄条不显示 | `scripts/check.mjs#R-01-018/AC-04`、`src/client.mjs::CSS` |
| 副作用 | 适用：卸载/窗格重建清理 click 监听；滚动条显隐既有行为不回归 | `scripts/check.mjs#R-02-003/AC-02`、`src/client.mjs::bindPaneControls` |

## 终态与证据

- 实现: `src/client.mjs` 新增 `TOP_THRESHOLD`（200px）常量、`.dap-top` 悬浮胶囊按钮 CSS（底部居中，窄条态隐藏）与骨架按钮（默认 hidden）；`bindPaneControls` 内 `syncTopBtn()` 单点同步显隐（onScroll 调用、窄条展开时同步一次），click 激活 `scrollTo` 回顶（`prefersReducedMotion()` 命中用 `auto`）；unbind 清理 click 监听。`.dsh-plugin/client.js` 已重建。
- 测试: `node scripts/check.mjs` 通过（新增 R-01-018/AC-01～AC-04 骨架/定位/显隐单点/展开同步/reduced-motion/窄条隐藏/监听清理断言，锚定 R-01-018 与 R-02-003/AC-02）；`pnpm build:client && pnpm check` 通过；`python3 tools/agentmap_lint.py --report` 通过（test-anchored=116/116）。`scripts/acceptance.mjs` 新增 R-01-018 人工验收步骤，真实滚动与悬浮表现仍需人工 GUI 验收。
- DESIGN 对照: PRD 新增 R-01-018（G-3 / 窗格渲染器），DESIGN 需求追溯索引恰好一行且「窗格渲染器」反向承接（职责与关键内部结构条目），与实现一致。
- commit: 9ff415794a1fcbcfd55d4f9493d330871b41fc6a
- commit: 9bd03f5654fc9a166ef15c6af55ddee3c9aa04bb
- commit: 7de72a75f81ad16f69d6392cb24f0870b7f92da5
- 编号说明: 本任务在 worktree 中以 T-064 立项开发；集成前 main 已被其它 worktree 推进并占用 T-064（速率校准进度），按未发布冲突规则 rebase 到最新 main 并重排为 T-065（文件名、frontmatter 与提交内引用同步）。
- review:
  - 审核方: Standards 子代理 `885f5acd-a7d1-450b-80a6-b3cc61c27f86`；Spec 子代理 `9146f9b2-ea21-4d0f-a7d8-f12c7115c4f9`。
  - 目的理解: 实现 R-01-018——窗格滚动超阈值（200px）后底部居中浮现「回到顶部」按钮，激活回顶（reduced-motion 直接定位），回阈值内隐藏，移动端抽屉同适用、桌面窄条不显示；复用既有 scroll 监听；unbind 清理监听（R-02-003）；测试锚定 AC-ID。
  - 执行方式: `code-review` skill；固定基线 `ea11dfb9ea27492499ced195cfd144d026604da4`，范围为 `git diff ea11dfb9ea27492499ced195cfd144d026604da4...HEAD` 的 T-065 工作单元（立项编号 T-064）；Standards/Spec 双轴并行审核，修复后由同一审核方分别复审。
  - 问题与修复: Standards 初审 1 处硬性——check.mjs 改写既有断言致 scrollHideTimer 清理回归证据丢失且消息与内容不符；修复为 unbind 顺序恢复邻接、原断言原样恢复、topBtn 清理独立断言承载（9bd03f5654fc9a166ef15c6af55ddee3c9aa04bb），并删除冗余 `.dap-top[hidden]` CSS。Spec 初审 1 处——AC-03 stale 边界：折叠窄条 display:none 可能使 scrollTop 归零而不派发 scroll 事件，展开后按钮残留；修复为显隐收敛 `syncTopBtn()` 单点并在窄条展开路径同步一次（7de72a75f81ad16f69d6392cb24f0870b7f92da5）。
  - 复审结论: Standards 复审通过（finding 清零、未引入新问题）；Spec 复审通过（两条 finding 清零，syncTopBtn 判空与展开同步时机核验无误）。
