#!/usr/bin/env bash
set -euo pipefail
export LC_ALL=C
root="$(git rev-parse --show-toplevel)"
for script in "$root"/.githooks/pre-commit.d/*.sh; do
  [[ -e "$script" ]] || continue
  "$script" "$@"
done
